# 2026-08-12 `iw`診断対応・アンテナ比較 引継ぎ

## 1. この文書の目的

この文書は、以下のJetson実機作業版で行ったPython改修、改修に至った経緯、確認済み動作、アンテナ比較結果を、別環境・前バージョンへ反映する担当者向けにまとめたものです。

```text
/home/triorb/projects/web_measurement_20260812
```

2026-07-28までのWi-Fi切断記録、SSID/BSSID固定、ポイント同期などの改修は [HANDOFF_20260728.md](HANDOFF_20260728.md) を参照してください。本書はその作業版を基に追加した2026-08-12分を扱います。

## 2. 改修の背景

従来CSVの`SignalStrength`は、`nmcli dev wifi`のスキャン一覧が返す0～100の品質値です。従来の`dBm`は、その品質値を次式で近似変換した値であり、Wi-Fiモジュールが接続リンクについて直接報告したRSSIではありません。

```text
dBm = SignalStrength / 2 - 100
```

この方式には次の制約がありました。

- 品質値が整数かつ段階的で、数dBのATTやケーブル損失の比較には粒度が粗い
- 現在接続中のStation統計ではないため、再試行・送信失敗・MCSなどが分からない
- RSSIが同程度でも通信品質が悪化した理由を説明しにくい
- `nmcli`スキャンが測定周期を数秒止める場合がある

そこで、既存の32カラムとWebプロットの互換性を維持したまま、Linux `iw`の接続Station情報をCSV末尾へ追加しました。

## 3. 実装方針

### 3.1 互換性

- 既存32カラムの名前、順序、値の生成方法は変更しない
- `iw`診断25カラムをCSV末尾へ追加し、合計57カラムとする
- 既存プロットは従来カラムだけを使い、旧CSVと新CSVを同じ操作で読み込めるようにする
- `iw`追加カラムは後解析用とし、現時点ではマッププロットへ追加しない
- `iw`取得に失敗しても従来測定は継続し、追加カラムだけを空欄にする
- 過去値による穴埋めを行わない

追加カラムの定義は [IW_DIAGNOSTICS.md](IW_DIAGNOSTICS.md) を参照してください。

### 3.2 取得タイミング

ローカル測定では、従来のWi-Fi情報取得とPingの後に次を取得します。

```bash
iw dev wlan0 station dump
iw dev wlan0 info
```

SSH測定では、従来の1回のSSHプローブ内で同じ情報を取得します。SSH接続回数は増やしていません。

各`iw`コマンドのタイムアウトは0.5秒です。取得不能時に測定ループを長時間止めないための上限です。

### 3.3 値の有効性確認

`nmcli`行のBSSIDと`iw station dump`のStation BSSIDが一致した場合だけ、`IwSampleValid=yes`として値を採用します。

次の場合は`iw`追加カラムを空欄にし、累積カウンタの差分計算もリセットします。

- `iw`取得失敗
- Wi-Fi切断中
- BSSID不一致
- Windows測定
- BSSID変更
- ドライバカウンタのリセットまたは巻き戻り

この処理により、切替前BSSIDや古いStation情報を現在行へ誤記録しません。

### 3.4 累積カウンタと差分

`tx packets`、`tx retries`、`tx failed`はドライバ累積値です。CSVには累積値に加えて、同一BSSIDの直前有効サンプルとの差分も記録します。

```text
IwTxRetryRatePct  = IwTxRetriesDelta / IwTxPacketsDelta * 100
IwTxFailedRatePct = IwTxFailedDelta  / IwTxPacketsDelta * 100
```

重要な注意点:

- `IwTxRetryRatePct`は「再試行されたパケットの割合」ではなく、「送信パケット数に対する再試行回数」です
- 1パケットが複数回再試行されるため100%を超えることがあります
- Ciscoなどが示すretransmission percentageと定義が一致するとは限りません
- 短い1サンプル単位の率は分母が小さく不安定です。区間評価では`sum(IwTxRetriesDelta) / sum(IwTxPacketsDelta)`を使用します
- `IwTxFailedDelta`は最終的な送信失敗の増分であり、Ping timeoutとは別の指標です

## 4. 測定停止問題と対策

### 4.1 最初に発生した問題

`iw`追加直後の試験では、0.5秒設定にもかかわらず、ポイント間に記録が2件程度しかない区間や、4～16秒の記録欠落が発生しました。

調査の結果、主因は`iw`ではなく、同期実行していたNeighborスキャンと通常の`nmcli dev wifi`でした。

### 4.2 Neighborスキャンの非同期化

Neighborスキャンを主測定ループとは別スレッドへ移しました。

- 主ループはRSSI、Ping、`iw`記録を継続
- Neighbor結果は完了後の測定行で取り込む
- 前回スキャンが実行中なら新しいスキャンを重ねない
- 測定セッションをまたいだ古い結果はgeneration番号で破棄
- Neighborの初期値はOFF

Neighbor ONは主測定周期を直接止めなくなりましたが、無線スキャン自体が接続通信へ与えるRF/ドライバ上の影響まではゼロになりません。アンテナ比較ではNeighbor OFFを推奨します。

### 4.3 NetworkManagerの暗黙再スキャン抑止

Neighbor OFFでも約30秒ごとに4～5秒の欠落が残りました。通常の接続情報取得に使っていた`nmcli dev wifi`が暗黙再スキャンを起こしていたためです。

接続情報取得コマンドを次へ変更しました。

```bash
nmcli --terse --fields ACTIVE,SSID,BSSID,CHAN,RATE,SIGNAL \
  dev wifi list --rescan no
```

これにより、NetworkManagerのキャッシュから接続情報を取得し、主測定ループ内で無線スキャンを開始しません。

### 4.4 修正前後の実測

| ログ | 件数 | 取得間隔中央値 | 最大間隔 | 1秒超の間隔 |
|---|---:|---:|---:|---:|
| `DB1-WRT-iw1_CWFant` 修正前 | 591 | 0.584秒 | 5.133秒 | 11回 |
| `DB1-WRT-iw2_CWFant` 修正後 | 699 | 0.577秒 | 0.762秒 | 0回 |

`iw2`は全699件で`IwSampleValid=yes`、BSSIDも全件一致しました。したがって`--rescan no`修正後は、追加診断による実用上の周期欠落は確認されていません。

Ping timeout行では、設定されたPing timeout時間だけ次行が遅れます。例えば約2.57秒の間隔が1件ある場合、2秒Ping timeout待ちに通常処理時間を加えた正常な挙動です。周期的な停止とは分けて判断してください。

## 5. 主な変更ファイル

### `web_measurement_app/logger_core.py`

- `LEGACY_HEADERS` 32列を維持し、`IW_HEADERS` 25列を末尾追加
- `iw station dump`解析を追加
- RSSI、chain signal、累積カウンタ、bitrate、MCS、NSS、帯域幅を抽出
- `iw dev wlan0 info`から設定TX powerを取得
- SSHプローブにも`iw`取得を追加
- 通常の`nmcli`一覧取得へ`list --rescan no`を追加
- Wi-Fi再接続間隔の初期値を15秒から5秒へ変更

### `web_measurement_app/service.py`

- ローカル測定で`iw`値を取得し、BSSID一致を検証
- 同一BSSIDの累積カウンタ差分と率を計算
- 切断、再接続、BSSID変更時に差分状態をリセット
- Neighborスキャンを非同期化
- 出力ディレクトリ初期値をプロジェクトルートへ変更
- Neighbor初期値をOFFへ変更

### `web_measurement_app/server.py`

- UI初期値の出力先をプロジェクトルートへ変更
- Wi-Fi再接続試行間隔の初期値を5秒へ変更
- Neighbor初期値をOFFへ変更

### `web_measurement_app/static/app.js` / `index.html`

- 再接続試行間隔の初期表示を5秒へ変更
- プロット形状の初期値を短冊（`strip`）へ変更
- 出力ディレクトリをサーバー設定値で初期化
- `iw`追加後も既存プロット仕様は変更しない

### `install_jetson.sh`

- `iw`コマンドを使用できるようJetson依存パッケージを導入

### `tests/test_iw_diagnostics.py`

以下を自動テストします。

- 新旧ヘッダー順序
- `iw station dump`の解析
- 同一BSSID時だけ差分を算出
- BSSID不一致時に古い値を書かない
- `nmcli`接続情報取得が`--rescan no`を使用
- デフォルト出力先、Neighbor OFF、再接続5秒

## 6. `iw`カラムの使い分け

| カラム | 推奨用途 | 注意 |
|---|---|---|
| `dBm` | 旧ログ・旧プロットとの互換比較 | `nmcli SIGNAL`からの近似値で粒度が粗い |
| `IwSignalDbm` | 直近の接続フレームRSSI、局所変化 | 瞬時値なのでばらつく |
| `IwSignalAvgDbm` | 接続通信フレームのドライバ平均 | ドライバの移動平均で測定区間と窓が一致しない場合がある |
| `IwBeaconSignalAvgDbm` | ATT、アンテナ、区間の安定比較 | 欠欄になるサンプルがあり得る |
| `IwChainSignalDbm` | チェーン別受信状態の確認 | 未接続側チェーンの弱い値をアンテナ性能値に使わない |
| `IwTxRetriesDelta` | 弱電・干渉による再試行増加 | 区間合計で評価する |
| `IwTxFailedDelta` | 最終的な送信失敗 | 0でも遅延や再試行が多い場合がある |
| `IwTxBitrate` / `IwRxBitrate` | レート適応、MCS低下の確認 | 瞬時値でありスループットではない |
| `IwTxPowerDbm` | インターフェース設定値の確認 | パケットごとの実送信電力ではない |

アンテナやATTの差は、各ポイントの`IwSignalDbm`または`IwBeaconSignalAvgDbm`平均を求め、ポイントごとの差を最後に等重み平均してください。全行単純平均だけでは、歩行速度やポイント滞在時間の差に重み付けされます。

## 7. Cisco指標と本ツールでの扱い

### 7.1 参照値

Cisco公式資料には、用途・製品に応じた次の目安があります。

| RSSI | Cisco資料上の説明・用途例 |
|---:|---|
| `-67 dBm`以上 | 音声など、遅延に敏感で高信頼な通信の設計目安 |
| `-70 dBm`程度 | 一般データ通信で信頼できるパケット配送の目安 |
| `-70～-80 dBm` | 弱い。性能問題が発生しやすい領域 |
| `-80 dBm`未満 | 非常に弱い。通信が不安定または失敗し得る領域 |

音声級の設計ではRSSIだけでなく、SNR 25 dB以上、Packet Error Rate 1%以下を併せて確認する考え方です。CiscoのVoWLAN RF Design Validationには、再送20%未満、packet loss 1%未満、channel utilization 50%未満などの目安もあります。

公式参照:

- [Cisco: Troubleshooting a Traditional Cisco Wireless Network](https://www.cisco.com/c/en/us/support/docs/smb/wireless/CB-Wireless-Mesh/1902-tz-Troubleshooting-Traditional-Cisco-Wireless-Network.html)
- [Cisco: VoWLAN RF Design Validation](https://www.cisco.com/c/en/us/td/docs/wireless/technology/vowlan/troubleshooting/vowlan_troubleshoot/8_Site_Survey_RF_Design_Valid.html)
- [Cisco: Enterprise Mobility Design Guide - Coverage Requirements](https://www.cisco.com/c/en/us/td/docs/wireless/controller/8-1/Enterprise-Mobility-8-1-Design-Guide/Enterprise_Mobility_8-1_Deployment_Guide/Chapter-11.html)
- [Cisco: IW9165E RSSI threshold](https://www.cisco.com/c/en/us/td/docs/wireless/outdoor/industrial/iw9165e/software/configuration/b-iw9165e-scg-17-18/m-manage-the-workgroup-bridges.html)

### 7.2 適用上の注意

これらはCisco製品・音声WLANなどの設計指標であり、AX210を搭載した本Jetson/AMRの絶対合否基準ではありません。本評価では、余裕度を見るための保守的な参考線として扱います。

- Cisco資料自身も、端末ごとにRSSI表示が異なると説明しています
- `-67 dBm`はノイズフロア`-92 dBm`、SNR 25 dBを前提とする例です
- 本CSVはノイズフロアとSNRを直接記録していません
- 本ツールの`IwTxRetryRatePct`はCiscoの再送率と定義が同一ではありません
- APが端末から受けるuplink RSSIは、このクライアント側ログだけでは評価できません
- RSSI単独ではなく、Ping timeout、遅延、再試行、送信失敗、切断を併記します

本プロジェクト内では、次の運用区分を目安にします。

| `iw` RSSI目安 | 扱い |
|---:|---|
| `-67 dBm`以上 | 高信頼・音声級のCisco参考線を満たす領域 |
| `-67～-70 dBm` | 一般データ通信として通常期待できる領域 |
| `-70～-80 dBm` | 弱電寄り。再試行、遅延、ローミング余裕を要確認 |
| `-80 dBm`未満 | 弱電評価領域。切断・再接続を含め重点確認 |

### 7.3 AMR/AGV向けの新しい外部指標

顧客へ必要な無線環境を説明する場合、前節のCisco一般指標だけを根拠にせず、
AMR/AGVを明示した比較的新しい資料を主な根拠として使用する。

| 出典 | 年 | 対象 | 主な指標 |
|---|---:|---|---|
| OMRON, *AMR Wireless Communication Technical Guide* | 2024 | OMRON AMR | 信号強度は理想`-40 dBm以上`、推奨最小`-60 dBm`。Ping RTTは推奨`10 ms以下`、最大許容`50 ms`とするメーカー固有の要求例。ただし、根拠となるSNR、MCS、測定区間、負荷、パーセンタイル、ローミング条件は示されていないため、本プロジェクトの顧客設備基準には採用しない |
| H3C, *WLAN Warehouse Scenario Deployment Guide-6W100* | 2025 | 倉庫AGV/AMR | AGV/AMRの信号強度`-65 dBm`、ローミング時の欠損`2パケット以下`、平均ローミング遅延`100 ms未満` |
| H3C, 同資料の導入例 | 2025 | AGV走行エリア | 主要カバレッジ`-65 dBm以上`、その他`-75 dBm以上`。95%のエリアで遅延`100 ms未満`かつ損失率`0.1%未満`。ローミング成功率`97%超`、平均ローミング遅延`100 ms未満`、ローミング損失率`1%未満` |
| Cisco, *Reliable Wireless for Factory AGV/AMR Environments* | 2022更新 | 工場・倉庫AGV/AMR | 低遅延、損失、ジッタ、handoff interruption、走行経路全体のカバレッジを主要KPIとする。用途によっては`50～70 ms`の短い通信断でもAGV停止につながり得る |
| NHS England Digital, *Assessing Wi-Fi performance metrics* | 現行Web資料 | 一般Wi-Fi | Primary RSSIは`-65 dBm以上`、Secondary RSSIは`-70 dBm以上`、SNRは`20 dB超`を推奨 |

公式参照:

- [OMRON: AMR Wireless Communication Technical Guide](https://files.omron.eu/downloads/latest/manual/en/amr_wireless_communication_technical_guide_technical_manual_en.pdf?v=1)
- [H3C: WLAN Warehouse Scenario Deployment Guide-6W100](https://www.h3c.com/en/Support/Resource_Center/EN/Home/Public/00-Public/Technical_Documents/Plan___Design/Plan___Design_Guides/WLAN_Warehouse_Scenario_Deployment-Long/202509/2657725_294551_0.htm)
- [Cisco: Industrial Automation - Reliable Wireless for Factory AGV/AMR Environments](https://www.cisco.com/c/en/us/td/docs/solutions/Verticals/Industrial_Automation/IA_Verticals/Factory/IA-Factory-CRD1/IA-Factory-CRD1.html)
- [Cisco: WLAN Site Survey Guidelines](https://www.cisco.com/c/en/us/support/docs/wireless/5500-series-wireless-controllers/116057-site-survey-guidelines-wlan-00.html)
- [NHS England Digital: Assessing Wi-Fi performance metrics](https://digital.nhs.uk/services/networks-and-connectivity-centre-of-excellence/connectivity-hub/advice-and-guidance/wi-fi-metrics-and-measures/assessing-wi-fi-performance-metrics/)

OMRONの`-40 dBm`は「ideal」とだけ記載された値であり、走行エリア全体に要求する
カバレッジ設計値としては過剰である。`-60 dBm`およびPing RTT `10/50 ms`も、
OMRON製AMRのメーカー要求例としては参照できるが、一般的なAMR設備基準を示す
根拠としては弱い。特に遅延は、Ping先、ネットワーク区間、負荷、パケットサイズ、
平均・最大・p95などの統計条件、ローミング中か否かで結果が変わる。

このため、本プロジェクトではOMRON値を顧客要求の主根拠にしない。2025年H3Cの
AGV/AMR向け`-65 dBm`とローミング・損失指標を要求案の中心にし、Ciscoの
`-67 dBm`、`-70 dBm`、`-80 dBm`区分とアクティブサイトサーベイ指針を補助根拠に
使用する。いずれも単一の業界規格ではないため、最終AMR構成による実通信試験を
合否判定に含める。

### 7.4 顧客向け無線環境要求のたたき台

次は外部資料と本実測を基にした初期案であり、契約仕様として使用する前に、製品側の
通信タイムアウト、必要周期、納入先AP構成、同時接続台数を含めて確定すること。

> AMRの通常走行経路、停止位置および充電位置において、最終製品と同じ無線モジュール、
> アンテナ、ATTおよびケーブル構成で、通常操業状態において次の条件を満たす
> 無線LAN環境を用意する。

| 項目 | 要求案 | 根拠・扱い |
|---|---:|---|
| 主要走行エリアのRSSI設計・受入値候補 | `-65 dBm以上` | H3C AGV/AMR主要カバレッジ。一般Wi-FiのPrimary RSSI推奨値とも整合 |
| Secondary RSSI | `-70 dBm以上` | AP切替余裕の参考。NHS現行指標 |
| SNR | `20 dB超`、高信頼用途では`25 dB以上`を目標 | NHS一般指標およびCisco音声級参考線 |
| パケット損失 | 95%のエリアで`0.1%未満` | H3C導入例 |
| ローミング成功率 | `97%超` | H3C導入例 |
| 平均ローミング遅延 | `100 ms未満` | H3C AGV/AMR指標。より高いリアルタイム性が必要なら`50 ms未満`を検討 |
| ローミング時の欠損 | `2パケット以下` | H3C AGV/AMR指標 |

受入試験は机上のAP配置計算やパッシブサーベイだけで完了とせず、実際のAMRまたは
同一無線機・アンテナを同じ高さと姿勢に設置した台車で、通常操業時の干渉源、設備、
搬送物、人員が存在する状態で実施する。Ciscoのサイトサーベイ指針でも、実端末が
APへ接続して通信、レート変更、再送、ローミングを行うアクティブサーベイが、
導入後検証に適している。

試験は目的別に分ける。

- BSSID固定試験: アンテナ、ATT、ケーブル、特定APからの弱電耐性を相対比較する
- SSID接続試験: BSSIDを固定せず、実運用と同じAP選択とローミングを検証する
- 実通信試験: RSSIだけでなくPing損失・遅延、`iw`再試行・失敗、切断時間を記録する

現在の比較ログはBSSID固定のため、アンテナ・ATT比較には使用できるが、ローミング
成功率とhandoff時間の合否判定には使用できない。顧客受入試験ではBSSID固定を解除した
別測定を必須とする。また、クライアントごとにRSSI表示差があるため、第三者のサイト
サーベイ値だけでなく、最終AMR構成で記録した`IwSignalAvgDbm`、
`IwBeaconSignalAvgDbm`および通信結果を最終判定に使用する。

## 8. 2026-08-12比較測定

### 8.1 共通条件

この2026-08-12版で`iw`カラムを追加してから取得した実測ログは、試験ログを含めてすべてOA側で取得しています。接続先は全行で`TriOrb-OA`、BSSIDは全行で`F4:D5:80:2D:24:81`です。以前の評価で使用した、場所によって意図的に弱電になる`TO-WAI2`のBSSIDで取得したログではありません。

- Jetson / AX210NGW
- 接続SSID: `TriOrb-OA`
- 固定BSSID: `F4:D5:80:2D:24:81`
- Channel: 116、80 MHz
- 同一ルート、P1からP15
- Neighbor OFF
- 台車は若いポイントから次ポイントへ同じ向きで移動
- アンテナは台車進行方向側の左端

主な比較ログ:

| 構成 | ログ |
|---|---|
| DB1-WRT、ATTなし | `DB1-WRT-iw2_CWFant__20260812_171523.csv` |
| DB1-WRT、4 dB ATTあり | `DB1-WRT-ATT4-iw3_CWFant__20260812_175642.csv` |
| TE 2118907-7 | `2118907-7-iw1-oa_CWFant__20260812_183639.csv` |

`iw`追加後に取得した次の4ログについて、CSV全行のSSID/BSSID一致を確認済みです。

- `DB1-WRT-iw1_CWFant__20260812_165521.csv`
- `DB1-WRT-iw2_CWFant__20260812_171523.csv`
- `DB1-WRT-ATT4-iw3_CWFant__20260812_175642.csv`
- `2118907-7-iw1-oa_CWFant__20260812_183639.csv`

各ログの`events.csv`はヘッダーのみで、測定中のSSID/BSSID切替イベントはありません。`neighbors.csv`もNeighbor OFFのためヘッダーのみです。

### 8.2 ATT実測

ATTなしWRTに対するATTありWRTのポイント等重み平均差:

| 指標 | ATTあり - ATTなし |
|---|---:|
| `IwSignalDbm` | `-3.77 dB` |
| `IwBeaconSignalAvgDbm` | `-3.55 dB` |
| 従来`dBm` | `-2.01 dB` |

`iw`では全15ポイントで低下し、公称4 dBに近い減衰を確認しました。従来`dBm`は粗い品質値換算のため、ATTの定量評価には使いません。

### 8.3 TEとの比較

ポイント等重み平均:

| 比較 | `IwSignalDbm` | `IwBeaconSignalAvgDbm` |
|---|---:|---:|
| TE - WRT ATTなし | `+2.39 dB` | `+2.85 dB` |
| TE - WRT ATTあり | `+6.17 dB` | `+6.39 dB` |

今回の条件では、TEがWRT ATTなしより約2.5～3 dB強く、WRTへ4 dB ATTを追加するとTEとの差は約6 dBでした。

### 8.4 一般・良好側と弱電側

P1～P4とP12～P15を一般・良好側、P5～P11を意図的な弱電側として区分しました。

一般・良好側:

| 構成 | `IwSignalDbm` | Beacon平均 | 再試行回数/送信パケット数 |
|---|---:|---:|---:|
| TE | `-54.02 dBm` | `-53.84 dBm` | `0.19` |
| WRT ATTなし | `-56.44 dBm` | `-56.74 dBm` | `0.44` |
| WRT ATTあり | `-60.64 dBm` | `-60.37 dBm` | `0.57` |

この区分では全構成でPing中央値は約6 ms、Ping timeout、Wi-Fi切断、`tx failed`は0でした。WRT ATTありもCiscoの一般データ通信参考線`-70 dBm`より十分強く、今回の環境では実用上問題ない結果です。

弱電側:

| 構成 | `IwSignalDbm` | Beacon平均 | 再試行回数/送信パケット数 |
|---|---:|---:|---:|
| TE | `-77.97 dBm` | `-78.77 dBm` | `0.41` |
| WRT ATTなし | `-80.33 dBm` | `-81.55 dBm` | `0.64` |
| WRT ATTあり | `-83.62 dBm` | `-85.00 dBm` | `1.11` |

WRT ATTありは弱電側で再試行と遅延が明確に増加しました。TEでもP8、WRT ATTありでもP5でPing timeoutが各1回あり、弱電環境そのものの影響も含まれます。いずれもWi-Fi切断と`tx failed`は0でした。

### 8.5 現時点の結論

- `iw`追加版は旧方式と同じRSSI/Ping傾向を維持し、取得周期欠落も修正済み
- 公称4 dB ATTの実効減衰を、接続リンクの`iw`値で約3.5～3.8 dBとして確認
- TEはWRT ATTなしより約2.5～3 dB良好
- WRT ATTありはTEより約6 dB低い
- 一般・良好な電波環境ではWRT ATTありでも安定しており、通常用途の実用範囲
- `-80 dBm`以下の弱電環境ではATTによる余裕低下が遅延・再試行に現れるため、納入先側のAP配置・電波環境改善が必要
- 「弱電でも必ず再接続できる」ことまでは、この良好BSSID測定だけでは保証しない

### 8.6 LMR-195 9 ft構成の追加評価（2026-08-13）

従来のLMR-400 8 ftからLMR-195 9 ftへ変更し、ATTなし・ATT4ありを各2回測定した。
全ログでSSID、BSSID、Channelは共通で、`IwSampleValid=yes`を確認済み。

```text
SSID: TriOrb-OA
BSSID: F4:D5:80:2D:24:81
Channel: 116
```

使用ログ:

| 構成 | ログ |
|---|---|
| LMR-195 9 ft、ATTなし1回目 | `DB1-WRT-195-9FT-iw1_CWFant__20260813_143058.csv` |
| LMR-195 9 ft、ATTなし2回目 | `DB1-WRT-195-9FT-iw2_CWFant__20260813_145826.csv` |
| LMR-195 9 ft、ATT4あり1回目 | `DB1-WRT-195-9FT-ATT4-iw1_CWFant__20260813_155257.csv` |
| LMR-195 9 ft、ATT4あり2回目 | `DB1-WRT-195-9FT-ATT4-iw2_CWFant__20260813_162528.csv` |

ATTなし1回目はP5で長く停止して239件を記録しているため、全行単純平均は使用しない。
各ログ内でP1～P15を先に平均し、同一構成の2測定を等重み平均し、最後に15ポイントを
等重み平均した。

#### LMR-400からLMR-195への変更

| 指標 | LMR-400 8 ft、ATTなし2回平均 | LMR-195 9 ft、ATTなし2回平均 | LMR-195による変化 |
|---|---:|---:|---:|
| `IwSignalDbm` | `-67.51 dBm` | `-69.30 dBm` | `-1.79 dB` |
| `IwBeaconSignalAvgDbm` | `-68.17 dBm` | `-69.94 dBm` | `-1.77 dB` |

Times Microwaveの5.8 GHz代表値では、LMR-195は`29.9 dB/100 ft`、LMR-400は
`10.8 dB/100 ft`である。9 ft LMR-195は約`2.69 dB`、8 ft LMR-400は約`0.86 dB`、
理論差は約`1.83 dB`となり、実測差`1.77～1.79 dB`と整合する。

#### LMR-195でのATT4有無

| 指標 | ATTなし2回平均 | ATT4あり2回平均 | ATT4による変化 |
|---|---:|---:|---:|
| `IwSignalDbm` | `-69.30 dBm` | `-72.12 dBm` | `-2.81 dB` |
| `IwBeaconSignalAvgDbm` | `-69.94 dBm` | `-72.91 dBm` | `-2.97 dB` |

ATT4あり2回の相互差は全体で約`0.2 dB`、ポイント単位の絶対差平均は約
`0.5～0.7 dB`で、歩行測定として再現している。公称4 dBより実測差が小さいが、
位置・姿勢差、マルチパス、ドライバ平均窓を含むため、ATTの校正値を否定する結果ではない。

| 環境区分 | ATTなし `IwSignalDbm` | ATT4あり `IwSignalDbm` | ATT4による変化 |
|---|---:|---:|---:|
| 一般・良好側（P1～P4、P12～P15） | `-58.22 dBm` | `-61.35 dBm` | `-3.12 dB` |
| 意図的な弱電側（P5～P11） | `-81.96 dBm` | `-84.42 dBm` | `-2.46 dB` |

ATT4あり2回合算では、Wi-Fi切断0件、Ping timeout 1件、`tx failed` 1件だった。
timeoutと送信失敗は`-85 dBm`未満の限界域で発生した。`-75～-80 dBm`では全53件の
Pingが成功したが、`-80 dBm`未満ではp95遅延と再試行が増える。したがって、
LMR-195 9 ft + ATT4は一般環境では実用範囲だが、`-80 dBm`未満を常用経路として
許容する評価にはしない。

### 8.7 MHF4L-SMA変換アセンブリ変更予定と暫定予測

MHF4LからSMAへの変換アセンブリを、TE Connectivity `2492541-2`へ変更する予定。
部品到着前のため、8.6節までのログは旧変換アセンブリで取得している。

予定部品:

- TE Connectivity `2492541-2`
- SMA bulkhead jack - MHF4L
- 1.37 mm low-loss coax、200 mm
- 50 ohm、12 GHz対応
- [TE製品ページ](https://www.te.com/en/product-2492541-2.html)
- [TE Product Specification 108-161932](https://www.te.com/commerce/DocumentDelivery/DDEController?Action=srchrtrv&DocFormat=pdf&DocLang=English&DocNm=108-161932&DocType=Specification+Or+Standard&PartCntxt=2492541-2)

TE仕様書では、`2492541-2`のケーブルアセンブリ全体の挿入損失上限を次のように規定する。

| 周波数範囲 | 挿入損失上限 |
|---|---:|
| DC～8 GHz | `1.70 dB max` |
| DC～12 GHz | `2.50 dB max` |

本測定の5 GHz帯は8 GHz以下なので、部品到着までは次の2ケースを併記する。

- 代表仮定: `1.0 dB`損失
- 保守仮定: 仕様上限の`1.7 dB`損失

重要: 新部品は旧変換アセンブリとの**交換**であり、追加接続ではない。現在の実測値には
旧変換アセンブリの損失がすでに含まれている。したがって、実際のRSSI変化は
`新アセンブリ損失 - 旧アセンブリ損失`であり、現在値から必ず`-1.0 dB`または
`-1.7 dB`低下するわけではない。以下は旧部品の損失を0 dBとみなして新部品の損失を
全量追加した、意図的に保守側の予測である。

#### `IwSignalDbm`の保守予測

| 構成・環境 | 現在の実測 | 代表仮定 `-1.0 dB` | 保守仮定 `-1.7 dB` | Cisco参考線による扱い |
|---|---:|---:|---:|---|
| LMR-195、ATTなし、一般・良好側 | `-58.22 dBm` | `-59.22 dBm` | `-59.92 dBm` | `-67 dBm`より十分強い |
| LMR-195、ATTなし、弱電側 | `-81.96 dBm` | `-82.96 dBm` | `-83.66 dBm` | 常用非推奨の弱電域 |
| LMR-195、ATT4あり、一般・良好側 | `-61.35 dBm` | `-62.35 dBm` | `-63.05 dBm` | 保守仮定でも`-67 dBm`に約4 dBの余裕 |
| LMR-195、ATT4あり、弱電側 | `-84.42 dBm` | `-85.42 dBm` | `-86.12 dBm` | timeout・送信失敗が発生し得る限界域 |

この保守予測でも、一般・良好側のLMR-195 9 ft + ATT4はCiscoの`-67 dBm`参考線を
満たし、暫定的には実用可能と評価する。一方、意図的な弱電側は新アセンブリの損失を
加味すると`-85 dBm`前後以下となるため、通信維持を保証する領域にはしない。

部品到着後は、旧・新アセンブリ以外を固定して最低2回ずつ測定し、各ポイントの
`IwSignalDbm`と`IwBeaconSignalAvgDbm`の差を等重み集計する。新旧差が確定した時点で、
上記の`-1.0/-1.7 dB`仮定を実測差へ置き換える。

### 8.8 LMR-240 9 ft構成の試験予定（2026-08-13）

LMR-195 9 ftより低損失で、LMR-400 8 ftより取り回しやすい候補として、次の
ケーブルアセンブリへ交換して同一ルートを測定する。

- McGill Microwave Systems `LMR-240-SF-SM-QTY-9ft`
- DigiKey品番 `20500463`
- 長さ: 9 ft（約2.743 m）
- コネクタ: SMA Female - SMA Male
- [DigiKey製品ページ](https://www.digikey.jp/ja/products/detail/mcgill-microwave-systems/LMR-240-SF-SM-QTY-9ft/20500463)
- [McGill LMR-240仕様](https://www.mcgillmicrowave.com/product/lmr-240/)

McGillの代表仕様値では、LMR-240の5.8 GHzにおける減衰は`66.8 dB/100 m`である。
この値から求めた9 ftのケーブル単体損失は約`1.83 dB`となる。比較は次のとおり。

| ケーブル | 長さ | 5.8 GHz仕様値 | 長さ換算のケーブル単体損失 | LMR-240との差 |
|---|---:|---:|---:|---:|
| LMR-400 | 8 ft | `10.8 dB/100 ft` | 約`0.86 dB` | LMR-240が約`0.97 dB`不利 |
| LMR-240 | 9 ft | `66.8 dB/100 m` | 約`1.83 dB` | 基準 |
| LMR-195 | 9 ft | `98.1 dB/100 m` | 約`2.69 dB` | LMR-240が約`0.86 dB`有利 |

したがってLMR-240は、LMR-195から大幅に改善するというより、理論上は約`0.9 dB`
改善する中間候補である。RSSIの走行間ばらつきは数dBあり得るため、1走行だけでは
この差を判定しない。LMR-195と同じSSID、BSSID、Channel、ルート、台車向き、
アンテナ位置で最低2回測定し、全行平均ではなく各ポイント等重みで比較する。

推奨ログ名は、ATTなしを`DB1-WRT-240-9FT-iw1_CWFant`、2回目を
`DB1-WRT-240-9FT-iw2_CWFant`とする。ATT4も評価する場合は
`DB1-WRT-240-9FT-ATT4-iw1_CWFant`とする。

上記の損失はケーブル本体の代表値であり、完成品の両端コネクタ、接続部のVSWR、
個体差は含まない。採用判断は仕様値だけでなく、次を確認して行う。

- LMR-195比の`IwSignalDbm`および`IwBeaconSignalAvgDbm`改善量
- 一般・良好側でCisco参考線`-67 dBm`に十分な余裕があること
- 弱電側の切断、ping timeout、`TxFailedDelta`、`TxRetriesDelta`の増減
- LMR-400に対する約`1 dB`の理論的不利が、取り回し・施工性の利点に見合うこと
- 新しいTE `2492541-2`を使う場合は、その実測差をケーブル差と分離すること

#### LMR-240 9 ft ATTなし実測（2026-08-13）

次の2走行を取得した。

- `DB1-WRT-240-9FT-iw1_CWFant__20260813_172035.csv`
- `DB1-WRT-240-9FT-iw2_CWFant__20260813_172817.csv`

両ログとも全行で次の接続状態を維持していた。

```text
SSID: TriOrb-OA
BSSID: F4:D5:80:2D:24:81
Channel: 116
IwStationBssid: F4:D5:80:2D:24:81
```

1回目721件、2回目662件の全サンプルで、従来`BSSID`と`IwStationBssid`は一致し、
`IwSampleValid=yes`だった。接続切替イベントはなく、測定後にNetworkManagerの
`TriOrb-OA`プロファイルにもBSSID `F4:D5:80:2D:24:81`の指定が保存されていることを
確認した。UIで固定操作を実行した記録は`events.csv`にないが、測定中のローミングや
別BSSID混入はないため、ケーブル比較データとして使用可能である。

各走行内でP1～P15を平均し、2走行を等重み平均した結果は次のとおり。

| 指標 | LMR-240 9 ft 1回目 | LMR-240 9 ft 2回目 | 2回平均 |
|---|---:|---:|---:|
| `IwSignalDbm` | `-68.02 dBm` | `-68.34 dBm` | `-68.18 dBm` |
| `IwBeaconSignalAvgDbm` | `-68.60 dBm` | `-68.60 dBm` | `-68.60 dBm` |

走行間差は`IwSignalDbm 0.33 dB`、Beacon平均`0.01 dB`で、再現性は良好だった。

| 比較 | `IwSignalDbm`差 | Beacon平均差 | 判定 |
|---|---:|---:|---|
| LMR-240 9 ft - LMR-195 9 ft | `+1.12 dB` | `+1.34 dB` | LMR-240が良好 |
| LMR-240 9 ft - LMR-400 8 ft | `-0.67 dB` | `-0.43 dB` | LMR-400がわずかに良好 |

LMR-195比の実測改善`1.1～1.3 dB`は、ケーブル仕様から予測した約`0.86 dB`と
方向および大きさが概ね整合する。LMR-400との差は`0.4～0.7 dB`に収まり、
仕様予測の約`0.97 dB`より小さかった。

| 環境区分 | LMR-240 `IwSignalDbm` | LMR-195との差 | LMR-400との差 |
|---|---:|---:|---:|
| 一般・良好側（P1～P4、P12～P15） | `-57.25 dBm` | `+0.97 dB` | `-0.89 dB` |
| 意図的な弱電側（P5～P11） | `-80.67 dBm` | `+1.30 dB` | `-0.41 dB` |

2走行合算でWi-Fi切断0件、Ping timeout 3件、`tx failed` 0件だった。timeoutは
P5、P6、P8で発生したが、その時点も固定BSSIDへの接続は維持されていた。弱電区間の
瞬間的なPing不達であり、BSSID固定不良を示すものではない。

現時点ではLMR-240 9 ftは、LMR-195より約1 dB改善し、LMR-400との差を1 dB未満に
抑えられる。取り回しや施工性がLMR-400より優れるなら、低損失と機械条件のバランスが
良い採用候補と評価できる。ATT4を必須とする最終構成なら、ATT4ありでも最低2回測定し、
一般・良好側の余裕と弱電側のtimeout・再試行を確認してから確定する。

2026-08-14に、日をまたいだ再現確認としてATTなし3回目も取得した。

- `DB1-WRT-240-9FT-iw3_CWFant__20260814_113509.csv`
- 実構成: LMR-240 9 ft、ATTなし（ファイル名どおり）
- 729件、P1～P15完遂
- `IwSignalDbm`: `-67.95 dBm`
- `IwBeaconSignalAvgDbm`: `-68.45 dBm`
- 前日のATTなし2回平均との差: `+0.22 dB`、Beaconは`+0.15 dB`
- Wi-Fi切断0件、Ping timeout 2件（P6、P7）、`tx failed` 0件

日付と時間帯が変わっても前日平均との差が約`0.2 dB`に収まり、LMR-240 ATTなしの
再現性を追加確認できた。次は同日の近い時間帯にATT4ありを測定し、この3回目を
時間帯対応のATTなし基準として比較する。

同日11:58開始でATT4ありも取得し、11:35開始のATTなし3回目と直接比較した。

- `DB1-WRT-240-9FT-ATT4-iw1_CWFant__20260814_115844.csv`
- 708件、P1～P15完遂
- 全件`TriOrb-OA / F4:D5:80:2D:24:81 / CH116`
- BSSID不一致0件、Wi-Fi切断0件、Ping timeout 2件（P5、P6）、`tx failed` 0件

| 指標 | 同日ATTなし | ATT4あり | ATT4による変化 |
|---|---:|---:|---:|
| `IwSignalDbm` | `-67.95 dBm` | `-71.79 dBm` | `-3.83 dB` |
| `IwSignalAvgDbm` | `-73.21 dBm` | `-76.23 dBm` | `-3.02 dB` |
| `IwBeaconSignalAvgDbm` | `-68.45 dBm` | `-72.58 dBm` | `-4.13 dB` |
| 従来`dBm` | `-74.08 dBm` | `-76.03 dBm` | `-1.96 dB` |

接続リンクの瞬時値とBeacon平均では公称4 dBと非常によく一致した。従来`dBm`は
`nmcli SIGNAL`からの粗い換算値で差を過小表示するため、ATTの定量評価には使用しない。

| 環境区分 | ATTなし `IwSignalDbm` | ATT4あり `IwSignalDbm` | 変化 | ATT4ありBeacon平均 |
|---|---:|---:|---:|---:|
| 一般・良好側（P1～P4、P12～P15） | `-56.56 dBm` | `-60.84 dBm` | `-4.28 dB` | `-60.74 dBm` |
| 意図的な弱電側（P5～P11） | `-80.98 dBm` | `-84.31 dBm` | `-3.32 dB` | `-86.11 dBm` |

一般・良好側はATT4ありでもCisco参考線`-67 dBm`に約6 dBの余裕があり、実用範囲と
評価できる。弱電側は`-84～-86 dBm`で通信限界域となり、再試行回数/送信パケット数も
ATTなし`0.34`からATT4あり`0.74`へ増加した。Ping timeoutは両条件とも2件で、切断と
送信失敗はなかったが、弱電側を常用環境として許容する結果にはしない。

同日13:27開始でATT4あり2回目も取得した。

- `DB1-WRT-240-9FT-ATT4-iw2_CWFant__20260814_132745.csv`
- 739件、P1～P15完遂
- 全件`TriOrb-OA / F4:D5:80:2D:24:81 / CH116`
- BSSID不一致0件、Wi-Fi切断0件、Ping timeout 3件、`tx failed` 0件

ATT4あり2回のポイント等重み平均差は`IwSignalDbm 0.34 dB`、Beacon平均`0.23 dB`、
ポイントごとの絶対差平均もそれぞれ`0.47 dB`、`0.43 dB`で、再現性は良好だった。
同日ATTなし1回に対するATT4あり2回平均は次のとおり。

| 指標 | 同日ATTなし | ATT4あり2回平均 | ATT4による変化 |
|---|---:|---:|---:|
| `IwSignalDbm` | `-67.95 dBm` | `-71.96 dBm` | `-4.00 dB` |
| `IwSignalAvgDbm` | `-73.21 dBm` | `-77.13 dBm` | `-3.92 dB` |
| `IwBeaconSignalAvgDbm` | `-68.45 dBm` | `-72.69 dBm` | `-4.24 dB` |
| 従来`dBm` | `-74.08 dBm` | `-76.08 dBm` | `-2.01 dB` |

| 環境区分 | ATTなし `IwSignalDbm` | ATT4あり2回平均 | 変化 | ATT4ありBeacon平均 |
|---|---:|---:|---:|---:|
| 一般・良好側（P1～P4、P12～P15） | `-56.56 dBm` | `-61.14 dBm` | `-4.58 dB` | `-60.95 dBm` |
| 意図的な弱電側（P5～P11） | `-80.98 dBm` | `-84.33 dBm` | `-3.34 dB` | `-86.11 dBm` |

2回目のPing timeout 3件中、P3の1件は`IwSignalDbm -59 dBm`で発生しており、
弱電だけでは説明できない単発のPing応答欠落である。残る2件はP5の弱電域だった。
ATT4あり2回合算でもWi-Fi切断と`tx failed`は0件で、timeout率は`5/1447`だった。
再試行回数/送信パケット数はATTなし`0.34`に対してATT4あり2回合算`0.70`となり、
弱電側のリンク余裕低下は確認できる。

最終評価として、LMR-240 9 ftにおけるATT4の実効減衰は接続リンクの`iw`値で
約`4.0～4.2 dB`と確定できる。一般・良好側は約`-61 dBm`で通常運用の余裕を維持するが、
意図的な弱電側は`-84～-86 dBm`で限界域となる。

### 8.9 LMR-400・LMR-240・LMR-195の最終比較

ATTなしはLMR-400を2回、LMR-240を3回、LMR-195を2回測定した。各走行内で
P1～P15を先に平均し、走行とポイントを等重みで集計した。

| ケーブル | 長さ | 5.8 GHz公称ケーブル損失 | `IwSignalDbm` | Beacon平均 | LMR-400比の実測低下 |
|---|---:|---:|---:|---:|---:|
| LMR-400 | 8 ft | 約`0.86 dB` | `-67.51 dBm` | `-68.17 dBm` | 基準 |
| LMR-240 | 9 ft | 約`1.83 dB` | `-68.10 dBm` | `-68.55 dBm` | 約`0.4～0.6 dB` |
| LMR-195 | 9 ft | 約`2.69 dB` | `-69.30 dBm` | `-69.94 dBm` | 約`1.8 dB` |

実測順位は公称値と同じく`LMR-400 > LMR-240 > LMR-195`となった。LMR-240は
LMR-195より`IwSignalDbm`で約`1.20 dB`、Beacon平均で約`1.39 dB`改善し、
LMR-400との差は約`0.5 dB`に収まった。

| ケーブル | ATT4あり測定数 | 一般・良好側 `IwSignalDbm` | 弱電側 `IwSignalDbm` | 弱電側Beacon平均 |
|---|---:|---:|---:|---:|
| LMR-400 | 1回 | `-60.64 dBm` | `-83.62 dBm` | `-85.00 dBm` |
| LMR-240 | 2回 | `-61.14 dBm` | `-84.33 dBm` | `-86.11 dBm` |
| LMR-195 | 2回 | `-61.35 dBm` | `-84.42 dBm` | `-86.54 dBm` |

ATT4ありの測定日時はケーブル間で異なるため、この表の小数dB差をそのままケーブル差とは
扱わない。一般・良好側では3種類とも約`-61 dBm`で、Cisco参考線`-67 dBm`に余裕があり、
実機システムとして使用可能だった。一方、弱電側は全種類で`-84～-86 dBm`となり、
ケーブル選定だけでは解決できない通信限界域である。

採用判断:

- LMR-400: RF性能は最良。太さ、取り回し、価格を許容し、損失最小を優先する場合に採用
- LMR-240: LMR-400比の低下が約`0.5 dB`だけで、LMR-195より約`1.2～1.4 dB`良い。性能と施工性のバランスが最良
- LMR-195: LMR-400比で約`1.8 dB`低いが、一般環境ではATT4込みでも実用範囲。ユーザー申告価格が高価品の約1/9であり、コスト優先なら合理的

したがって、弱電環境を納入先側のAP配置・無線設計で改善する前提なら、3種類とも一般環境で
使用可能である。余裕と取り回しを両立する第一候補はLMR-240、価格最優先はLMR-195、
損失最優先はLMR-400と整理する。

この評価はケーブル単体をVNAで測定した絶対挿入損失ではなく、アンテナ、変換部品、AX210、
ドライバを含む実機システムの同一ルート相対比較である。顧客向けには、部品単体仕様の保証と
実運用時の通信成立性評価を分けて記載する。

## 9. 次担当者の反映・確認手順

1. `logger_core.py`の旧32ヘッダー順序を変えず、`IW_HEADERS`を末尾追加する
2. `service.py`のBSSID一致検証とカウンタリセット条件を省略しない
3. Neighborスキャンを主測定スレッドへ戻さない
4. 通常の`nmcli`取得から`--rescan no`を外さない
5. Python 3.8互換を維持する
6. `iw`未導入環境でも従来測定が継続することを確認する
7. 以下のテストを実行する

```bash
cd /home/triorb/projects/web_measurement_20260812
.venv/bin/python -m unittest discover -s tests -v
.venv/bin/python -m py_compile web_measurement_app/*.py
```

8. Jetson実機で40秒以上測定し、以下を確認する

```text
57カラム
IwSampleValid=yes
BSSIDとIwStationBssidが一致
通常時の最大取得間隔が1秒未満
Neighbor OFF時はneighbors.csvがヘッダーのみ
```

9. アンテナ比較ではSSID、BSSID、Channel、ルート、台車向き、アンテナ位置、Neighbor設定を固定する
10. 評価は全行平均ではなくポイント平均差を基本とし、良好・通常・弱電の環境区分を分ける

## 10. 未実装・残課題

- SNRとノイズフロアの直接記録
- channel utilizationの記録
- AP側で見たJetsonのuplink RSSI、retry、PERの取得
- `iw`診断値のマッププロット
- 本ツールのretry指標とIEEE/Cisco定義に合わせた再送パケット率の取得
- 7.4節の顧客向け要求案を、製品通信仕様と納入先条件に合わせて契約上の受入基準へ確定
- TE `2492541-2`到着後の旧・新変換アセンブリ直接比較と、暫定`1.0/1.7 dB`仮定の更新
- LMR-240 9 ftを同一条件で最低2回測定し、LMR-195 9 ftおよびLMR-400 8 ftと比較

追加プロットを実装する場合も、まずCSV後解析で有用性を確認してからにしてください。列追加だけで従来プロットを重くしないことを優先しています。
