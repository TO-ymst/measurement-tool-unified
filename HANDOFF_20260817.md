# 2026-08-17 Ping統計・Survey診断 引継ぎ

## 1. 対象とGit情報

この文書は、2026-08-12版の`iw`診断対応に続いて追加した診断機能を、Jetson側のCodexへ引き継ぐための資料です。

- リポジトリ: `git@github.com:TO-ymst/measurement-tool-unified.git`
- ブランチ: `feature/ping-stats-survey-diagnostics`
- 実装コミット: `1aa7349 Add ping statistics and async survey diagnostics`
- PC側の作業ディレクトリ: `C:\Users\YamashitaKoji\OneDrive - TriOrb\04_スクリプト関連\電測スクリプト統合版`

前回の`iw`診断、Neighborスキャン非同期化、`nmcli --rescan no`化の内容は
[HANDOFF_20260812.md](HANDOFF_20260812.md) を参照すること。
Wi-Fi切断記録、SSID/BSSID固定、再接続、Point操作の内容は
[HANDOFF_20260728.md](HANDOFF_20260728.md) を参照すること。

## 2. 今回追加した機能

### 2.1 Pingのローリング統計

既存のPing結果を一定時間窓で集計し、各主CSV行の末尾に記録する。
追加のPingパケットは送信しない。

初期値は30秒、UIでは5～300秒で変更可能である。

| カラム | 内容 |
|---|---|
| `PingWindowSamples` | 時間窓内のPing試行数 |
| `PingWindowSuccesses` | 成功したPing数 |
| `PingLossRatePct` | Ping timeout率 (%) |
| `PingRttMeanMs` | RTT平均値 |
| `PingRttMedianMs` | RTT中央値 |
| `PingRttMinMs` / `PingRttMaxMs` | RTT最小値 / 最大値 |
| `PingRttP95Ms` / `PingRttP99Ms` | RTTの95 / 99パーセンタイル |
| `PingJitterMeanMs` | 連続した成功Ping間のRTT差分絶対値の平均 |

`Status=ok`、`ping_timeout`、`timeout`だけをPing試行として数える。
`wifi_disconnected`、SSH失敗、Wi-Fi情報取得失敗は、Wi-Fi到達性の異常として扱い、Ping損失率の分母には含めない。

### 2.2 測定周期の診断

主CSVの末尾へ次を追加する。

| カラム | 内容 |
|---|---|
| `SampleDurationMs` | その測定行の取得処理に要した時間 |
| `SamplePeriodMs` | 前回サンプル開始から今回サンプル開始までの実間隔 |
| `ScheduleOverrunMs` | 取得処理時間が設定測定間隔を超えた分。超えない場合は`0` |

これにより、Ping timeout、コマンド待ち、ドライバ応答遅延などでログ周期が崩れたかをCSV上で確認できる。
通常の待機時間を含めた「次行までの総時間」は`SamplePeriodMs`で確認する。

### 2.3 任意の`iw survey dump`取得

Webダッシュボードの「診断統計」で`iw survey dump を取得`をONにした場合だけ取得する。初期値はOFFであり、従来どおりの測定では実行されない。

| 設定 | 初期値 | 内容 |
|---|---:|---|
| `survey_enabled` | OFF | Survey取得の有効化 |
| `survey_interval_sec` | 5秒 | Survey取得の最短開始間隔。UIでは1～3600秒 |

取得元:

- Jetsonローカル測定: `iw dev wlan0 survey dump`
- WindowsからのJetson SSH測定: Jetson上で同コマンドを実行する追加SSH接続
- Windowsローカル測定: 値は空欄

追加カラム:

| カラム | 内容 |
|---|---|
| `SurveySampleValid` | 現在使用中チャネルのSurvey情報を取得できた場合は`yes` |
| `SurveyUpdated` | その主CSV行で新しいSurvey結果を受け取った場合は`yes` |
| `SurveyAgeMs` | Survey取得完了からの経過時間 |
| `SurveyFrequencyMhz` | 使用中チャネル周波数 |
| `SurveyNoiseDbm` | ドライバが返すノイズ値 |
| `SurveyActiveMsDelta` | チャネル稼働時間カウンタの差分 |
| `SurveyBusyMsDelta` | チャネルBusy時間カウンタの差分 |
| `SurveyRxMsDelta` / `SurveyTxMsDelta` | チャネルRx / Tx時間カウンタの差分 |
| `SurveyBusyRatePct` | `SurveyBusyMsDelta / SurveyActiveMsDelta * 100` |

`SurveyBusyRatePct`は同一周波数で有効な連続カウンタを取得できたときだけ算出する。周波数変更、カウンタ巻き戻り、対応していないWi-Fiドライバでは差分列を空欄にする。

## 3. 互換性と画面への影響

- 従来32列と既存`iw`25列、合計57列の名前・順序・値生成は変更していない。
- 今回の23列は57列の後ろに追加するため、新しい主CSVは合計80列となる。
- 旧CSVをダッシュボードへ読み込む動作は変更していない。
- 新しい列を使う地図モード、リアルタイムプロット、PNG出力は追加していない。
- 位置紐づけ、スループット、CSI、スペクトラム解析は今回の対象外である。
- 画面には「診断統計」設定欄だけを追加した。測定モード、AP/BSSID固定、ローカル/SSH測定、Neighbor、Pingターゲットの既存機能は変更していない。

このため、アンテナ比較などの既存運用は従来の列だけで継続できる。新しい診断値は原因分析用の補助情報として使う。

## 4. 性能とリアルタイム性

### 4.1 既定状態

SurveyはOFFである。この状態で増える処理は、既に取得済みのPing値をPythonで集計し、CSV末尾へ23列を書き出すことだけである。無線スキャン、追加Ping、追加SSHは行わない。

### 4.2 SurveyをONにした場合

Survey取得は主測定ループと別のdaemonスレッドで実行する。

- 実行中のSurveyがある間は新しいSurveyを開始しない。
- 主測定ループはSurvey完了を待たない。
- 測定停止・再開始をまたいだ古いSurvey結果はgeneration番号で破棄する。
- ローカル`iw`のタイムアウトは0.5秒、SSH Surveyのタイムアウトは5秒である。ただし、いずれもバックグラウンド処理である。

従って、Surveyが遅い・失敗する場合でも主CSVのRSSI/Ping/`iw station dump`取得やWebSocket配信を直接待たせない。ただし、Wi-FiドライバやJetsonのCPU負荷を完全にゼロにはできないため、アンテナ比較の基準ログではSurvey OFFを推奨する。Surveyを使う調査では、`SamplePeriodMs`と`ScheduleOverrunMs`を併記して実測上の影響を確認する。

## 5. 何が新たに判断できるか

前バージョンにも`IwSignalAvgDbm`、再送/失敗率、MCS/NSS、Tx/Rx bitrate、単発Pingは含まれている。今回新たに判断しやすくなる点は次の通り。

1. 単発Pingではなく、区間としての損失率、遅延の裾野、ジッタ。
2. 記録の空白や表示遅れが、電波現象か測定処理遅延かの切り分け。
3. RSSIが同程度でも、チャネルBusy率やノイズが高いことによる品質低下の仮説確認。

Surveyのノイズ/BUSY値はWi-Fiドライバ依存であり、AP側のチャネル利用率や絶対的な干渉量と同一視しないこと。原因の確定ではなく、追加の切り分け材料として使用する。

## 6. Jetson側での確認手順

1. 上記ブランチを取得して、Jetsonの作業版へ反映する。
2. Jetsonでは`iw`コマンドが利用できることを確認する。

   ```bash
   iw dev wlan0 station dump
   iw dev wlan0 survey dump
   ```

3. 通常測定を開始し、主CSVヘッダー末尾に`PingWindowSamples`から`SurveyBusyRatePct`までの23列があることを確認する。
4. Survey OFFで1分程度測定し、`SamplePeriodMs`が従来と同等であることを確認する。
5. 追加調査が必要なときだけダッシュボードでSurveyをONにし、`SurveySampleValid=yes`と`SurveyUpdated=yes`が一定間隔で出ることを確認する。
6. ドライバがSurveyを返さない場合は、主測定を止めずSurvey列が空欄のままであることを確認する。

WindowsからJetsonをSSH測定する場合、Survey ON時のみ5秒ごとを上限として追加SSHが発生する。USBまたは有線LANでJetsonへ接続している状態で検証すること。

## 7. 実装ファイルとテスト

| ファイル | 変更内容 |
|---|---|
| `web_measurement_app/logger_core.py` | 新CSVヘッダー、`iw survey dump`の解析・ローカル/SSH取得 |
| `web_measurement_app/service.py` | Ping窓集計、実行時間計測、Surveyワーカー、CSV末尾追加 |
| `web_measurement_app/server.py` | 新設定の初期値API |
| `web_measurement_app/static/index.html` | 診断統計の設定欄 |
| `web_measurement_app/static/app.js` | 設定の読込・送信 |
| `web_measurement_app/static/styles.css` | 診断設定欄の表示順 |
| `tests/test_iw_diagnostics.py` | ヘッダー互換性、Ping統計、Survey解析・Busy率・既定OFFのテスト |

PC側で実行済みの検証:

```powershell
python -m compileall -q web_measurement_app tests
python -m unittest discover -s tests -v
```

結果: 11 tests passed。

## 8. Jetson Codexへの作業指示

- 既存の測定周期、`nmcli --rescan no`、Neighbor非同期化、AP/BSSID固定、Wi-Fi切断時の再接続を後退させないこと。
- 変更時は、既存57列を途中変更せず、新規列は必ず末尾へ追加すること。
- Survey処理を主測定スレッドへ戻さないこと。
- 新しいプロットや地図表示を追加する場合は、既存のCSV取込とPNG全レジェンド出力への影響を別途テストすること。
- Jetson実機で確認する場合は、まずSurvey OFFで基準周期を取り、その後にSurvey ONの影響を`SamplePeriodMs`で比較すること。

## 9. Jetson実機確認結果（2026-08-17）

LMR-240 9 ft + ATT4、Survey OFF、Neighbor OFFで、前版と同一ルートを測定した。

- 新ログ: `DB1-WRT-240-9FT-ATT4-iw3-v0817_CWFant__20260817_102908.csv`
- 比較対象:
  - `DB1-WRT-240-9FT-ATT4-iw1_CWFant__20260814_115844.csv`
  - `DB1-WRT-240-9FT-ATT4-iw2_CWFant__20260814_132745.csv`
- SSID: `TriOrb-OA`
- BSSID: `F4:D5:80:2D:24:81`
- Channel: 116

### 9.1 CSV互換性と基本動作

- 652行、Index 0～651が連続し、P1～P15も昇順で完遂
- 全行80列で、先頭57列の名前と順序は旧版と完全一致
- 全行`IwSampleValid=yes`
- 全行で従来`BSSID`と`IwStationBssid`が一致
- Wi-Fi切断0件、`tx failed` 0件、Ping timeout 1件
- Neighbor OFFのためneighbors.csvはヘッダーのみ
- 接続切替がないためevents.csvはヘッダーのみ
- Survey OFFのためSurvey 10列は全行空欄
- `PingJitterMeanMs`の先頭行と`SamplePeriodMs`の先頭行が空欄なのは、比較対象となる前サンプルがないため正常

### 9.2 前版との比較

各ログ内でポイント平均を先に求め、旧版2回を等重み平均した。

| 指標 | 旧版2回平均 | 2026-08-17版 | 新版 - 旧版 |
|---|---:|---:|---:|
| `IwSignalDbm` | `-71.96 dBm` | `-71.18 dBm` | `+0.78 dB` |
| `IwSignalAvgDbm` | `-77.13 dBm` | `-77.68 dBm` | `-0.55 dB` |
| `IwBeaconSignalAvgDbm` | `-72.69 dBm` | `-72.47 dBm` | `+0.22 dB` |
| 従来`dBm` | `-76.09 dBm` | `-75.68 dBm` | `+0.41 dB` |

ポイント単位の絶対差平均は`IwSignalDbm 1.10 dB`、Beacon平均`0.72 dB`で、通常の
走行・マルチパス変動の範囲である。RSSI取得に改修による回帰は認められない。

成功Pingだけを集計した新版の全体値は、中央値`5.40 ms`、p95 `17.85 ms`、
p99 `36.00 ms`だった。旧版2回の中央値は`8.01 ms`と`7.04 ms`で、新版に遅延悪化はない。
再試行回数/送信パケット数も新版`0.63`、旧版`0.74`と`0.67`で同程度である。

### 9.3 Pingローリング統計から分かったこと

30秒窓は通常52～53試行を保持した。今回の全測定ではPing試行652件中、成功651件、
timeout 1件で、全体損失率は`0.153%`だった。

timeoutはP6で発生し、その行は次の状態だった。

```text
IwSignalDbm=-86 dBm
IwSignalAvgDbm=-91 dBm
IwBeaconSignalAvgDbm=-90 dBm
PingWindowSamples=48
PingWindowSuccesses=47
PingLossRatePct=2.083%
```

P8ではtimeoutではないがRTT `289 ms`の外れ値があり、その後の30秒窓で
`PingRttP99Ms=164.968 ms`、`PingJitterMeanMs`最大`18.440 ms`を記録した。
全体中央値だけでは見えない弱電区間の遅延裾野を、P95/P99/Jitterで確認できている。

環境区分別の成功Pingは次のとおり。

| 区分 | RTT中央値 | p95 | p99 | 最大 |
|---|---:|---:|---:|---:|
| 一般・良好側 | `5.17 ms` | `10.60 ms` | `24.70 ms` | `44.0 ms` |
| 弱電側 | `5.87 ms` | `27.35 ms` | `44.58 ms` | `289.0 ms` |

中央値の差は小さいが、弱電側では上位遅延とジッタが明確に悪化する。新しいPing統計は
アンテナの平均RSSI評価よりも、通信品質の瞬間劣化と裾野の確認に有用である。

### 9.4 測定周期診断

| 指標 | 中央値 | p95 | p99 | 最大 |
|---|---:|---:|---:|---:|
| `SampleDurationMs` | `78.049 ms` | `93.526 ms` | `112.041 ms` | `2067.529 ms` |
| `SamplePeriodMs` | `579.080 ms` | `594.514 ms` | `613.238 ms` | `2568.427 ms` |

Ping timeout行だけ`SampleDurationMs=2067.529 ms`、`ScheduleOverrunMs=1567.529 ms`となり、
その次のサンプル開始までが約`2568 ms`になった。それ以外に説明不能な大きな空白はない。
ログが一時停止したように見える場合、今回についてはUIやCSV書込み停止ではなく、
Ping timeout待ちが原因と判定できる。

前版の実周期中央値は577～578 ms、新版は579 msであり、診断列追加による周期悪化はない。
この確認後、設定間隔をstart-to-start周期として扱うように修正した。現在はサンプル開始時刻から
処理時間を差し引いた残り時間だけ待機する。設定500 ms、取得処理約78 msなら待機は約422 msとなり、
実周期は約500 msになる。処理が500 msを超えた場合は追加待機せず、直後に次のサンプルを開始する。

ここまでの記述は非同期化前の挙動である。この修正だけではPingが遅い区間ほど空間サンプル数が
減る問題が残るため、後述のローカルPing非同期化を追加した。現在の挙動は「10. ローカルPing非同期化」
を参照すること。

### 9.5 Surveyと既存iw列の注意点

Jetson実機で`iw dev wlan0 survey dump`を実行したが、現在のAX210/ドライバでは出力が
空だった。この環境でSurveyをONにしても`SurveySampleValid=yes`、Noise、Busy率を
取得できない可能性が高い。Survey OFF時の非同期制御と空欄出力は正常だが、Survey値の
実機取得は未確認である。対応ドライバの無線デバイスで別途検証が必要になる。

既存列`IwChannelWidthMhz`は弱電側を中心に108行空欄だった。該当する`iw` bitrate文字列は
幅表記を含まないHEレートで、パーサーが幅を推定しないためである。`IwSampleValid`、MCS、
NSS、bitrate自体は取得できており、新版の回帰ではない。必要なら、幅表記省略時を20 MHzと
みなせるか、実際の`iw`仕様を確認して別改修する。

### 9.6 自動テスト

Jetson上で次を実行し、非同期PingとRSSI表示切替を含む19テスト成功とコンパイル成功を確認した。

```bash
.venv/bin/python -m unittest discover -s tests -v
.venv/bin/python -m py_compile web_measurement_app/*.py
```

## 10. ローカルPing非同期化（2026-08-17）

### 10.1 実施に至った経緯

このツールの本来の目的はアンテナ単体比較ではなく、台車を移動させながら空間の電波状況を
地図上で把握することである。アンテナ比較は、この用途に合う既存ツールを転用した副次的な利用である。

従来は1サンプル内でWi-Fi情報取得、Ping完了待ち、CSV書込みを直列実行していた。設定間隔0.5秒、
Ping timeout 2秒の場合、弱電区間でPingがtimeoutすると約2秒間は次のRSSI・`iw`測定も開始されない。
このため、詳しく測る必要がある弱電区間ほど行数が減り、次の問題が発生する。

- 短い遮蔽物区間がtimeout 1行だけになり、一瞬の異常に見える
- ポイント間を等速移動しても、サンプルを等間隔配置したプロットと実距離が一致しない
- 弱電区間のRSSI変動、遅延の連続性、区間長を過小評価する
- 条件ごとにサンプル密度が変わり、空間比較が不公平になる

この問題を解消するため、ローカル測定ではPingだけを非同期化し、Wi-Fi/RSSI取得周期をPing応答時間から
分離した。SSHリモート測定は複合SSH取得の互換性を優先し、従来の同期方式を維持している。

### 10.2 実装方式

各ローカルサンプルは開始時に次の値を固定する。

- `Index`
- `Point`
- 日付・時刻
- `nmcli`のSSID、BSSID、Signal、従来dBm
- `iw`のRSSI、MCS、NSS、送信再試行・失敗カウンタ等

Pingは同じ`Index`を付けてワーカープールへ渡す。Ping完了順が前後しても結果は開始時Indexへ戻す。
CSVへ書いた過去行を後から直接編集せず、未確定行をメモリで保持し、Ping確定後にIndex順でCSVへ書く。
これによりCSV破損、行順逆転、Ping完了時点の別ポイントへの誤記録を防ぐ。

リアルタイム表示は次の二段階となる。

1. Wi-Fi情報取得直後に`status=ping_pending`として同じIndexを表示
2. Ping成功またはtimeout時に、WebSocketで同じIndexを置換更新

Pingプロットではpendingを一時的に灰色で表示し、成功時は測定値の色、timeout時は赤へ更新する。
RSSI、Signal、BSSID等のプロットはPing待ちに関係なく取得直後から通常表示する。CSVには
`ping_pending`は残らず、既存80列の名前・順序も変更していない。

測定停止時は実行中Pingを最大timeoutまで待ち、全結果をIndex順でCSVへ確定してからファイルを閉じる。
旧測定から遅れて返った結果は世代番号で破棄し、新測定の同一Indexへ混入させない。

### 10.3 測定間隔

自由入力を廃止し、次の5択とした。デフォルトは0.5秒である。API側でも選択肢外を拒否する。

| 設定 | 表示上の用途 |
|---:|---|
| 0.2秒 | 高密度 |
| 0.5秒 | 標準 |
| 1秒 | 負荷軽減 |
| 2秒 | 低頻度 |
| 5秒 | 定点測定 |

当初は`0.5秒（推奨）`、`1秒（標準）`と表示していたが、`0.5秒`を推奨とする規格や厳密な
ベンチマーク根拠はない。実機で通常の取得処理が約65～80 msであり、一時的な処理遅延への余裕、
空間分解能、ログ量のバランスが取りやすい実用上のデフォルトという位置付けである。このため表示を
`0.5秒（標準）`、`1秒（負荷軽減）`へ変更した。

`広範囲用`という表示は採用していない。測定範囲が広くても、局所的な弱電区間を検出するには高い
サンプル密度が必要であり、適切な間隔は範囲の広さだけでは決まらない。移動測定では、概ね
`サンプル間の移動距離 = 移動速度 × 測定間隔`として判断する。例えば秒速1 mの台車では、
0.2秒で約0.2 m、0.5秒で約0.5 m、1秒で約1 mごとの取得になる。

測定時間だけを理由に1秒へ変更する必須条件もない。2026-08-17に取得した実ログ
`DB1-WRT-240-9FT-ATT4-iw3-v0817_CWFant__20260817_102908.csv`は、652行、281,272 bytesで、
1行平均は約431 bytesだった。この値を使った概算は次のとおりである。列内容により実容量は変動する。

| 間隔 | 1時間あたりの行数 | 1時間あたりのCSV容量目安 |
|---:|---:|---:|
| 0.2秒 | 18,000行 | 約7.8 MB |
| 0.5秒 | 7,200行 | 約3.1 MB |
| 1秒 | 3,600行 | 約1.6 MB |

既定の`保持行数`は20,000行であり、バックエンドのリアルタイム履歴はポイント単位でこの上限を適用する。
同じポイントに留まり続けた場合、0.2秒で約1時間7分、0.5秒で約2時間47分、1秒で約5時間33分に
相当する。上限を超えると古いリアルタイム履歴から削除されるが、CSVファイルへの記録は削除されず
全行残る。

通常の空間測定は0.5秒を基準とする。細かな局所変化を優先する短時間測定では0.2秒、数時間以上の
長時間測定で位置分解能より処理・描画負荷軽減を優先する場合は1秒を検討する。ただし、移動測定で
局所的な弱電区間を捉える必要がある場合は、1秒へ粗くするより、0.5秒を維持してエリアやルート単位で
ログを分割する方が適切である。

`nmcli`・`iw`取得自体は重複実行しない。これらの処理時間が設定間隔を超えた場合は、そのサンプル完了直後に
次を開始し、超過を`ScheduleOverrunMs`へ残す。非同期化後の`SampleDurationMs`はPing待ちを含まず、
そのサンプルのWi-Fi情報取得処理時間を表す。`SamplePeriodMs`は実際のサンプル開始間隔である。

### 10.4 得られるメリット

- Pingが2秒timeoutする環境でも、RSSI・`iw`は設定周期で取得を継続できる
- 弱電区間だけログ密度が落ちる偏りを防止できる
- 等速移動時のサンプル位置とプロット上の距離対応が改善する
- 遮蔽物等による連続悪化を複数行として捉え、単発外れ値と区別しやすくなる
- Ping結果、RSSI、開始時ポイントの対応をIndexで追跡できる
- 停止時や完了順逆転時もCSVのIndex順と列互換性を維持できる

長いRTTやtimeoutは移動中の複数位置にまたがる現象であり、Ping開始位置へ割り当てても物理的な位置の
不確かさは残る。ただし、従来のようにその待ち時間中のRSSIサンプル自体が消えることはない。

### 10.5 Jetson実機確認

到達しないテスト用アドレスへPingし、0.2秒間隔、timeout 2000 ms、Survey OFF、Neighbor OFFで確認した。

- 1秒時点: 5行すべて`ping_pending`
- 3.4秒時点: 17行（timeout確定7行、pending 10行）まで取得継続
- 停止後CSV: 18行、80列、Index 0～17連続
- CSV内`ping_pending`: 0行
- 全18行: `ping_timeout`
- `SamplePeriodMs`中央値: `200.220 ms`、最大`200.418 ms`
- `SampleDurationMs`中央値: `65.416 ms`、最大`68.754 ms`
- 最終30秒窓: 18試行、損失率100%

同条件の従来方式では約2秒に1行となるところ、非同期化後は約0.2秒周期を維持した。

別の実機テストではPing待機中にP1からP2へ進めて停止した。CSVは3行で、ポイントは
`Index 0=P1、Index 1=P2、Index 2=P2`、全行timeout、pending残存0、エラー0だった。
Ping完了時の現在ポイントへ付け替わるズレがないことを確認した。

### 10.6 主な変更ファイル

| ファイル | 内容 |
|---|---|
| `web_measurement_app/service.py` | Pingワーカープール、Index紐付け、履歴upsert、順序保証CSV、停止時回収 |
| `web_measurement_app/static/app.js` | WebSocket行のIndex置換、pending表示、プロット再描画 |
| `web_measurement_app/static/index.html` | 測定間隔を5段階の選択式へ変更 |
| `web_measurement_app/logger_core.py` | デフォルト測定間隔を0.5秒へ変更 |
| `tests/test_iw_diagnostics.py` | 完了順逆転、世代違い、間隔選択肢、周期制御のテスト |

## 11. ログ名区切りと画像タイトル（2026-08-17）

- ログPrefixとログBase名の間には、入力時の末尾・先頭の`_`有無にかかわらず、必ず1個の`_`を入れる。
- Prefixが空の場合はBase名だけを使用し、先頭に`_`は付けない。
- 画像タイトルは「ログファイル名を使用」をデフォルトとし、現在のCSVファイル名から`.csv`だけ除いた文字列を使う。
- 長すぎる場合は「ログ Prefixを使用」を選択できる。測定開始時のPrefixを使い、区切り用の末尾`_`はタイトルから除く。
- 必要な場合は「自由入力」を選び、従来どおり任意タイトルを指定できる。
- この選択は単体プロット画像と全レジェンド画像の両方へ適用される。

## 12. RSSIプロットの区別（2026-08-17）

- `接続中AP RSSI（iw・空間測定用）`を追加した。CSVの既存列`IwSignalDbm`を使用し、追加取得処理はない。
- Wi-Fiドライバが現在接続しているBSSIDについて返す値で、取得周期ごとの空間変化確認に使用する。
- 従来プロットは`従来RSSI換算値（nmcli・過去ログ比較用）`へ改名した。
- 従来値はWi-Fi一覧の`SIGNAL（%）`から換算した値で、スキャンキャッシュにより更新が遅れる場合がある。
- 正しい比較条件（BSSID `F4:D5:80:2D:24:81`、CH116）の2026-08-17 0.2秒ログでは、従来dBmは1,858行中52回だけ変化し、最長90行（約18秒）同値だった。一方、`IwSignalDbm`は1,337回変化し、最長同値でも12行（約2.4秒）だった。
- 先に確認へ使用した`DB1-WRT-240-9FT-ATT4-iw4-v0817-async-02s`はBSSID `F4:D5:80:2D:23:61`へ接続していたため、TEや過去のOAログとのアンテナ比較から除外する。ただし、非同期Pingの周期確認用ログとしては有効である。
- 両モードを画面選択と全モード画像に残し、過去ログとの互換比較と今後の空間測定を両立する。

## 13. iw導入後のアンテナ・ATT評価見直し（2026-08-17）

### 13.1 評価を見直した経緯

従来のRSSIプロットは、`nmcli dev wifi`のスキャン一覧にある`SIGNAL（%）`をdBmへ換算した値を
使用していた。この値は現在接続中リンクの瞬時RSSIそのものではなく、変換式とスキャンキャッシュの
影響を受ける。実ログでは強電側を実際より弱く、弱電側を実際より強く見せ、変動幅を圧縮していた。

このため、従来はATTや細径ケーブルを含む構成について「通常エリアでも下限まで4 dB程度しか余裕がない」
ように見えていた。一方、AX210ドライバが現在接続中BSSIDについて返す`IwSignalDbm`で確認すると、
通常エリアの絶対値はnmcli換算値より良好だった。ただし、構成間の相対差まで小さくなったわけではない。
iwで比較すると、ATTとケーブルによる低下量自体はnmcliより明確に確認できた。

したがって、評価結果は次のように整理する。

> ATT・ケーブルによる受信強度低下は存在し、ATT4の追加低下は概ね公称どおりである。ただし、通常の
> 電波環境では低下後にも従来想定より大きい絶対RSSIの余裕があり、実用上は想定ほど深刻ではない。

### 13.2 比較条件

今回の比較は、ポイントごとの滞在行数や0.2秒・0.5秒の取得密度差に引きずられないよう、各ログの
ポイント別中央値を求めてから条件間で比較した。台車は若いポイントから次ポイントへ同じ方向を向けて
移動し、アンテナは進行方向左端に設置している。

- 接続SSID: `TriOrb-OA`
- 比較対象BSSID: `F4:D5:80:2D:24:81`
- チャンネル: CH116
- TE現行: `2118907-7-iw1-oa_CWFant__20260812_183639.csv`（1回）
- WRT + LMR240 9FT、ATTなし: 3回
- WRT + LMR240 9FT、ATT4あり: 過去3回と今回の合計4回
- 今回の確認ログ: `DB1-WRT-240-9FT-ATT4-BSSID2481-iw5-v0817-async-02s_CWFant_20260817_140006.csv`

TE現行はiw列を持つ比較可能ログが1回だけである。このため、以下は実ルートにおける有力な比較結果では
あるが、アンテナ単体の公称利得や実験室での挿入損失を確定する測定ではない。WRT側の差にはアンテナ、
変換アダプタ、ケーブル、ATT、設置位置および測定時の環境変動を含む。

### 13.3 今回ログの健全性

今回の正しい24:81固定ログは、改修後ツールの基本動作確認にも使用できる。

| 項目 | 結果 |
|---|---:|
| 行数 | 1,858行 |
| 測定時間 | 約371.8秒 |
| Point | P1からP15まで順序正常 |
| SSID / BSSID | 全行`TriOrb-OA` / `F4:D5:80:2D:24:81` |
| Channel / Rate | 全行CH116 / 540 Mbit/s（nmcli列） |
| `IwSampleValid` | 1,858 / 1,858行 |
| `SamplePeriodMs`中央値 | 200.227 ms |
| `SamplePeriodMs`最大 | 200.961 ms |
| `ScheduleOverrunMs` | 全行0 ms |
| Status | ok 1,855行、ping_timeout 3行 |
| CSV内`ping_pending` | 0行 |

Ping timeoutはP4、P5、P6で各1件発生したが、約0.2秒周期を維持した。旧同期ログ
`DB1-WRT-240-9FT-ATT4-iw3-v0817_CWFant__20260817_102908.csv`では、Ping timeout時に
`SamplePeriodMs`が最大2,568 msまで伸びていた。非同期化後は悪いPingの待ち時間中もRSSI・iw取得を
継続できており、改修目的を満たしている。

今回ログのポイント別`IwSignalDbm`は、同一構成の過去3回の中央値に対して、15ポイント平均で
`-0.33 dB`、差の中央値で`0 dB`、範囲で`-2～+1 dB`だった。外れ値ではなく、再現性は良好である。

### 13.4 iwとnmcli換算値の実測差

同じ行でも両者は同じ測定量ではなく、固定オフセットで変換できない。

| ポイント | `IwSignalDbm`中央値 | nmcli従来dBm中央値 | iw - nmcli |
|---|---:|---:|---:|
| P1 | -50 dBm | -65.0 dBm | +15.0 dB |
| P2 | -57 dBm | -68.5 dBm | +11.5 dB |
| P3 | -69 dBm | -74.5 dBm | +5.5 dB |
| P5 | -83 dBm | -80.5 dBm | -2.5 dB |
| P6 | -87 dBm | -83.0 dBm | -4.0 dB |
| P14 | -52 dBm | -67.0 dBm | +15.0 dB |

強電・通常域ではiwの方が大幅に良く、弱電域では逆にiwの方が悪くなる。nmcli換算値は強弱の差を
圧縮しているため、今後の空間測定とアンテナ評価には`IwSignalDbm`を使う。nmcli従来dBmは過去ログとの
互換比較専用として残す。

### 13.5 TE、LMR240、ATT4の比較結果

15ポイントを同じ重みで比較した結果は次のとおりである。

| 構成 | TE現行比（iw） | TE現行比（nmcli従来値） |
|---|---:|---:|
| WRT + LMR240 9FT、ATTなし | 約-3.1 dB | 約-1.4 dB |
| WRT + LMR240 9FT、ATT4あり | 約-6.9 dB | 約-3.3 dB |
| ATT4による追加低下 | 約-3.9 dB | 約-1.9 dB |

iwで確認したATT4の追加低下約3.9 dBは、公称4 dBと整合する。従来nmcli換算値はATTの影響も約2 dBに
圧縮していた。したがって、「ATTやケーブルによる損失がほぼない」とは評価しない。正しくは、損失は
存在するが、通常環境で受信下限を割るほどではない、という評価になる。

ルート内の比較的良好・通常側の区間としてP1～P4およびP12～P15を集計すると次のとおりである。

| 構成 | iwポイント中央値の区間平均 | TE現行比 |
|---|---:|---:|
| TE現行 | -54.4 dBm | 基準 |
| WRT + LMR240 9FT、ATTなし | -57.4 dBm | -3.0 dB |
| WRT + LMR240 9FT、ATT4あり | -61.5 dBm | -7.1 dB |

弱電側のP5～P11では、WRT + LMR240 + ATT4が平均約`-84.6 dBm`となり、再送・遅延が増える。これは
納入先側で避けるべき無線環境として別管理する。通常環境での実用性評価と、意図的に選んだ弱いBSSIDでの
限界評価を混同しない。

### 13.6 顧客へ求める-65 dBmの解釈

先方へ「全エリアで-65 dBm前後を確保してほしい」と要求する場合、可能であれば次の最終搭載状態で
Jetsonの`iw`を使って確認する。

- 実採用アンテナ
- 実採用の変換アダプタ
- 実採用ケーブル長・ケーブル種類
- ATT4
- 実際の台車上の設置位置・向き

最終構成のJetsonで`IwSignalDbm >= -65 dBm`を全走行エリアで満たすなら、仮に通常運用上の目安を
`-70 dBm`とした場合は約5 dB、`-75 dBm`とした場合は約10 dBの受信余裕がある。従来nmcli換算値では
同じ通常区間が`-68～-70 dBm`程度に見え、余裕を約4～7 dBと小さく見積もっていた。実機iwで見る限り、
通常環境の絶対RSSIには従来想定より余裕がある。

ただし、先方が別メーカーのサーベイ端末やPCで測定した`-65 dBm`と、最終構成Jetsonの
`IwSignalDbm=-65 dBm`は同一とは限らない。端末、アンテナ、向き、筐体、測定位置による差があるため、
別端末のサーベイ値だけを受入判定に使う場合は、事前にJetson実測値とのオフセットと再現性を確認する。

顧客向けには次の表現が妥当である。

> 従来のnmcli換算値では受信マージンを小さく見積もっていた。実際の搭載構成で取得したiw RSSIにより、
> ATT4およびケーブルによる低下は確認されたが、全エリアで実機iw RSSIを-65 dBm前後以上に確保できれば、
> 通常運用に対して従来想定より十分な受信マージンがある。

### 13.7 新しい診断列から分かったことと残課題

- 今回ログの`IwTxPacketsDelta`合計は2,207、`IwTxRetriesDelta`合計は1,853、`IwTxFailedDelta`合計は2だった。再試行は1パケットに複数回発生するため100%を超える区間があり、パケット損失率とは解釈しない。
- P5～P10ではRSSI低下とともに再試行・Ping遅延が増え、単なるRSSI表示差ではなくリンク品質も悪化していることを確認できた。
- Ping中央値は9.18 ms、p95は約69.9 msだった。旧同期方式は悪いPingほど次サンプルを止めていたため、新方式でp95が高く見えることは、悪化区間を正しく高密度取得できた結果の可能性がある。
- `IwTxMcs`・`IwTxNss`の空欄は、送信レートが54または18 Mbit/sのレガシーOFDM表記となり、MCS/NSSが存在しない行で発生している。パース不良ではない。
- 2チェーン値を取得できた行ではチェーン間RSSIが大きく非対称で、片側だけアンテナを接続している構成と整合する。ただし、iwのチェーン番号と物理MAIN端子の対応はこのログだけでは断定しない。
- `_events.csv`がヘッダのみなのは切断・再接続がなく、`_neighbors.csv`がヘッダのみなのはNeighbor OFFだったためである。今回ログでは実際の切断後再接続機能までは検証していない。
- Survey列は全行空欄だった。実機AX210環境では`iw survey dump`が有効値を返さず、現状は実質未対応である。SurveyをONにしても値がない場合、画面上で未対応と明示する改修余地がある。

### 13.8 現時点の改修内容まとめ

- 従来32列を先頭に維持し、iw、Pingローリング統計、周期診断、任意Survey列を後方追加した。
- `IwSignalDbm`を空間測定用RSSIとしてリアルタイム画面、CSV読込み、単体画像、全モード画像で選択可能にした。
- nmcli換算RSSIは`従来RSSI換算値（nmcli・過去ログ比較用）`と明記した。
- ローカルPingを非同期化し、Ping timeout中もRSSI・iwを設定周期で取得するようにした。
- 非同期Ping結果を開始時Indexへ戻し、完了順にかかわらずCSVをIndex順で確定するようにした。
- 測定間隔を0.2、0.5、1、2、5秒の選択式とし、0.5秒を標準にした。
- PrefixとBase名の間へアンダースコアを必ず1個だけ挿入するようにした。
- 画像タイトルをログファイル名、ログPrefix、自由入力から選択可能にした。
- 自動テスト19件で、旧列互換、非同期結果のIndex対応、世代違い結果の破棄、周期制御、iw表示を確認した。
