# 2026-07-28 改修内容・前バージョン反映用メモ

この文書は、Jetson上の以下の作業版で行った改修を、PC側にある前バージョンへ反映するための引き継ぎ資料です。

```text
/home/triorb/projects/web_measurement_20260728
```

## 対象環境

- Jetson: `orin-temp-0515`
- Python: 3.8
- NetworkManager / nmcli: 1.22.10
- Webサーバー: Uvicorn、`0.0.0.0:9000`
- Jetsonへの画面接続: USB Ethernet経由の `http://192.168.55.1:9000`

## 改修概要

1. Wi-Fi切断中も測定ループとポイント操作を止めず、`wifi_disconnected`を継続記録するように変更
2. Ping timeout、Wi-Fi切断、データ欠測をプロット上で明確に区別
3. CSV内でPoint番号が欠けた区間も「No data」として描画
4. BSSID切替・固定を測定開始前にも実行可能に変更
5. Jetson上でBSSIDを確実に切り替えるため、接続プロファイル固定後に再接続する方式へ変更
6. SSID固定とBSSID固定を区別できるよう画面文言を整理
7. Python 3.8で起動できるよう型注釈などを互換化
8. BSSID固定中の切断時は、固定先の可視性を短周期で確認してから再接続する方式へ変更
9. プロット再描画の重さや測定再開始でPoint区間がずれないよう、Point更新順序と画面状態を修正
10. 測定中は対象SSID以外へ接続しない厳密なSSID固定と、停止時の設定復元を追加

## 変更ファイル

### `web_measurement_app/service.py`

Wi-Fi再接続を測定スレッドから分離しました。

- `_local_reconnect_thread`を追加
- 再接続中も測定周期ごとに`wifi_disconnected`行を記録
- 再接続中でも`advance_point()`を操作可能
- `_write_local_disconnected_record()`へ切断行生成を集約
- Wi-Fi切断中は不要なネイバースキャンとPingを実行しない
- NetworkManager操作中も測定ループを長時間ブロックしない
- 測定開始前でもBSSID切替・固定APIを利用可能
- BSSID固定は「直前にアプリで切り替えたBSSID」ではなく、現在接続中のBSSIDを対象に変更
- `exclusive_ssid_during_measurement`がONの場合、ローカル測定開始時に厳密なSSID固定を自動適用
- 測定が所有するSSID固定は停止時に自動解除し、以前の自動接続設定へ復元
- 測定開始前から手動固定されていた場合は、停止時に解除せず維持

切断行の主な内容:

```text
Status=wifi_disconnected
time_ms=<wifi_disconnected_value、標準設定は999>
Error=wifi_disconnected または再接続中の状態
```

ローカル再接続はバックグラウンドスレッドで実行し、成功・失敗は`*_events.csv`へ記録します。

### `web_measurement_app/logger_core.py`

BSSID切替方法を変更しました。

旧方式:

```bash
nmcli device wifi connect <SSID> bssid <BSSID>
```

または:

```bash
nmcli connection up id <PROFILE> ap <BSSID>
```

JetsonのNetworkManager 1.22.10では、上記が成功終了しても同一SSIDの元BSSIDへ接続したままになる場合がありました。

新方式:

1. 現在の接続プロファイルを取得
2. `802-11-wireless.bssid`へ対象BSSIDを設定
3. 接続プロファイルを一度切断
4. 同じ接続プロファイルを再接続
5. 実際の接続BSSIDを検証
6. 失敗時は以前のBSSID固定値へ戻して再接続

実際の処理に相当するコマンド:

```bash
nmcli connection modify id TO-WAI2 802-11-wireless.bssid 30:DE:4B:4A:12:67
nmcli connection down id TO-WAI2
nmcli --wait 30 connection up id TO-WAI2
```

あわせて、nmcli 1.22が返す`30\:DE\:...`形式を通常の`30:DE:...`へ正規化しています。

注意:

- 「選択したBSSIDへ切替・固定」は、切替とプロファイル固定を同時に行います。
- 固定解除時は`802-11-wireless.bssid`を空に戻します。
- Wi-Fi経由で画面を開いている場合は操作中に画面接続が切れます。USBまたは有線LAN経由で操作してください。

#### BSSID固定中の高速再接続

従来は、切断のたびに以下を順番に実行していました。

1. `nmcli --wait 30 connection up`
2. 失敗時に汎用の`nmcli device wifi connect`

画面の再接続間隔を5秒にしても、各コマンドの待ち時間により実際の試行間隔が約25～50秒になる場合がありました。

新方式では、保存済み接続プロファイルにBSSID固定値がある場合だけ以下の処理を行います。

1. Wi-Fiデバイスを特定
2. 切断中にデバイス単位のWi-Fi再スキャンを実行
3. 固定BSSIDが見えなければ接続処理を開始せず、次の再接続周期へ戻る
4. 固定BSSIDが見えた場合だけ、同じ固定済みプロファイルを最大15秒待ちで再接続
5. 接続SSIDとBSSIDの両方を検証
6. BSSID固定中は別BSSIDへ接続し得る汎用フォールバックを実行しない

Jetson実機では再スキャンと可視性判定が約5秒で完了しました。再接続間隔が5秒の設定なら、固定先が見えない間もおおむね5秒ごとに確認できます。

この処理はバックグラウンド再接続スレッド内だけの変更です。測定周期、Ping、RSSI取得、CSVの数値変換、`wifi_disconnected`記録、Point操作には変更を加えていません。また、再スキャンは切断中だけ実行するため、接続中のPing測定をスキャンで乱さない設計です。

NetworkManagerが自動スキャン中の場合、`nmcli device wifi rescan`は以下のエラーを返すことがあります。

```text
Scanning not allowed while already scanning
Scanning not allowed immediately following previous scan
Scanning not allowed while unavailable or activating
```

これらは致命的エラーにせず、`nmcli device wifi list --rescan no`で進行中スキャンのキャッシュを確認します。固定BSSIDがキャッシュに見えれば接続へ進み、見えなければ次の再接続周期へ戻ります。権限不足など他のエラーは従来どおり失敗として記録します。

### `web_measurement_app/static/app.js`

#### 切断・欠測表示

ステータス色をメトリクス値より優先して描画します。

| 状態 | 色 | 表示 |
|---|---|---|
| 通常 | 従来のメトリクス色 | Ping / RSSIなどのカラースケール |
| `ping_timeout` / `timeout` | `#ff4d22` | 赤橙 |
| `wifi_disconnected` | `#424851` | 濃いグレー。点表示では四角 |
| `no_data` | `#b8bec6` | 明るいグレー。区間全体を線表示 |

- 状態凡例を画面とPNG出力の両方へ追加
- 点表示でも欠測区間全体が分かるよう、始点から終点まで線を描画
- 測定中に区間ログが0件でも`no_data`区間を生成
- Point付きCSV取込時は、観測済み最大Pointまでの欠番を`no_data`区間として生成
- PC基準ログとJetsonログの時刻照合取込でも欠番区間を生成

#### BSSID操作

- 測定開始前でも「選択したBSSIDへ切替・固定」を操作可能
- 一度「選択したSSIDへ接続」を押していなくても、現在SSIDのBSSID候補を操作可能
- 選択BSSIDが現在BSSIDと一致した場合だけ「現在のBSSIDを固定」を有効化
- BSSID切替ボタンの処理内容を「切替・固定」として表示

#### Point区間の同期

従来はクリック座標追加と全プロット再描画の後に`/api/advance-point`を実行していました。区間とログが増えて再描画が重くなると、クリック後もロガーが一つ前のPointへ記録し続ける時間が発生します。

また、新しい測定を開始しても前セッションの`points`と`segments`が画面側に残るため、「開始→停止→再開始」などで始点が一つずれる場合がありました。

新方式:

1. 新しい測定の開始成功時に、前セッションの座標・区間・画面ログをクリア
2. 測定中の2点目以降は、再描画より先に`/api/advance-point`を実行
3. 区間の始点・終点をクリック時に確定し、Point番号から画面配列を引き直さない
4. Point更新完了まで次のクリックと取り消し操作を抑止
5. Point更新失敗時は追加した終点を画面から戻す
6. 測定停止中の座標入力は、Point付きCSV取込用として従来どおり利用可能

ブラウザキャッシュ対策として、`index.html`の`app.js`バージョンを`20260728_pointsync`へ更新しています。

厳密なSSID固定追加後の最新バージョンは`20260729_ssidlock2`です。

### `web_measurement_app/static/index.html`

誤解を避けるため、画面文言を変更しました。

| 旧文言 | 新文言 |
|---|---|
| Jetson AP切替 / BSSIDテスト | Jetson Wi-Fi接続 / SSID・BSSID固定 |
| 接続先AP (SSID) | 接続先SSID |
| 候補APを更新 | Wi-Fi候補を更新 |
| 選択したAPへ切替 | 選択したSSIDへ接続 |
| 現在のAPを固定 | 現在のSSIDに固定 |
| AP固定を解除 | SSID固定を解除・復元 |
| 選択したBSSIDへ切替 | 選択したBSSIDへ切替・固定 |

SSID固定は、対象以外の保存済みWi-Fiプロファイルを`autoconnect=no`にして、対象SSIDが圏外でも別SSIDへ逃げないようにします。BSSID固定は、対象SSID内で接続する物理アクセスポイントを指定します。

### 厳密なSSID固定

`logger_core.py`の`set_local_wifi_ap_lock()`を、優先度変更から排他的固定へ変更しました。

固定時:

1. 保存済みWi-FiプロファイルのUUID、`autoconnect`、優先度を取得
2. 元設定を`logs/ssid_lock_state.json`へ保存
3. 対象プロファイルを`autoconnect=yes`、優先度100へ設定
4. 対象以外のWi-Fiプロファイルを`autoconnect=no`へ設定
5. 対象外SSIDへ接続中なら、その接続を切断
6. 全プロファイルの状態を再取得して固定状態を検証

解除時:

1. スナップショット内の全プロファイルをUUIDで特定
2. `autoconnect`と優先度を固定前の値へ復元
3. 全復元成功後にスナップショットを削除

設定途中で失敗した場合も復元を試みます。スナップショットはサーバープロセス外に残るため、異常終了後も画面の「SSID固定を解除・復元」から復元できます。

### `web_measurement_app/server.py`

Python 3.8対応として、`str | None`などPython 3.10以降の型注釈を`Optional[...]`へ変更しています。

前バージョンがPython 3.8対応済みなら、この変更は不要です。

### `FEATURES.md`

今回の仕様に合わせて、Wi-Fi接続、厳密なSSID固定、BSSID切替・固定に関する説明を更新しました。

## 確認済み動作

### Wi-Fi切断記録

モック再接続を0.8秒遅延させた状態で、切断行の書き込みが約0.001秒で戻ることを確認しました。

測定ループ統合テストでは以下を確認しています。

```text
wifi_disconnected行: 8行
切断中のPoint 1 -> Point 2更新: 成功
切断中の記録継続: 成功
```

### BSSID切替・固定

Jetson実機で以下を確認しています。

```text
SSID: TO-WAI2
BSSID: 30:DE:4B:4A:12:67
Channel: 36
接続プロファイルのBSSID固定: 30:DE:4B:4A:12:67
```

`iw dev wlan0 link`と`nmcli connection show TO-WAI2`の両方で確認済みです。

### BSSID固定中の再接続

- 固定BSSIDが見える場合: 最大15秒待ちの固定済みプロファイル接続へ進むことをモックで確認
- 固定BSSIDが見えない場合: `connection up`や汎用SSID接続を実行せず終了することをモックで確認
- NetworkManagerのスキャン中エラー時: キャッシュに固定BSSIDがあれば可視と判定し、なければ未検出で戻ることをモックで確認
- 権限不足などスキャン中以外のエラー: 握りつぶさず失敗として返すことをモックで確認
- Jetson実機: `wlan0`、固定値`30:DE:4B:4A:12:67`、固定先の可視性を正常取得
- Python 3.8の`py_compile`: 成功

### サーバー

- Pythonファイルの`py_compile`: 成功
- `/api/status`: HTTP 200
- `/api/local/access-points`: 現在BSSIDと固定値を正常返却
- 接続先SSID候補は、現在のスキャン結果に保存済みWi-FiプロファイルのSSIDも
  加えて返す。電波が一時的に見えない保存済みSSIDも選択可能。
- SSID固定ボタンは選択中SSIDへの接続成功後にだけ固定する。選択したSSIDと
  現在接続中SSIDが違う状態で、現在側を誤固定しない。
- BSSID候補は現在接続中SSIDではなく、接続先SSID欄の選択値に連動する。
  保存済み固定BSSIDがスキャンで見えない場合も「保存済み・現在未検出」と表示。
- 測定パラメータのSSIDはブラウザ読込時の実接続SSIDで初期化し、Wi-Fi未接続
  の場合は既定値へフォールバックせず空欄にする。
- Wi-Fi候補更新はSSID/BSSIDの強制再スキャンを行う。選択値とは別に、実際の
  接続SSID/BSSIDを常時表示する。
- Wi-Fi接続・固定操作は成功/失敗を色付きで明示する。失敗後は接続状態を
  再取得し、実際に接続が残っているSSID/BSSIDもエラー文へ併記する。
- SSID接続・SSID固定はBSSID未指定プロファイルを優先し、保存済みBSSID固定値
  を接続条件に使わない。BSSID指定・固定は下段のBSSID操作だけで行う。
- 測定開始時のSSID固定は実接続SSIDとの一致を検証し、実際に接続中の
  プロファイルを固定する。不一致なら測定を開始しない。
- `/`および`/static/app.js`: HTTP 200
- 配信HTMLが`/static/app.js?v=20260728_pointsync`を参照することを確認
- 厳密なSSID固定追加後は`/static/app.js?v=20260729_ssidlock2`を参照

### 厳密なSSID固定

- モック: 対象以外の自動接続停止、対象外の現在接続切断、固定状態検証、元設定復元に成功
- 実機手動操作: `TriOrb-OA`以外の9プロファイルを停止し、解除後に全プロファイル設定が変更前と完全一致
- 実機測定開始・停止: 開始時に9プロファイル停止、停止時に自動復元、スナップショット削除を確認
- API: `/api/local/fix-ap`と`/api/local/clear-ap-fix`で固定・復元に成功
- 異常終了対策: `logs/ssid_lock_state.json`が残っていればサーバー再起動後も状態確認・復元可能

### Point区間同期

- `DB1-WRT-4_CWFant__20260728_182812.csv`はPoint 1～15が順番どおりで、欠番・逆行なし
- 同ログの`*_events.csv`には再接続イベントなし
- Point更新APIがプロット再描画より先に実行されることをソース検査で確認
- 新規測定開始時に前セッションの座標・区間・画面ログをクリア
- Point更新中の重複クリックと取り消しを抑止

## 前バージョンへ反映する手順

PC側の前バージョンへは、フォルダ全体を上書きするより、以下の順で差分移植する方が安全です。

このJetson側フォルダには`.git`がないため、コミット差分は取得できません。PC側のGit管理済み前バージョンと、このフォルダの対象ファイルを比較して移植してください。

1. `service.py`の非同期再接続と切断行記録を移植
2. `logger_core.py`のSSID固定・復元、BSSID切替・固定、固定中の高速再接続を移植
3. `app.js`のSSID固定UI、ステータス色、欠測区間、凡例、BSSID活性条件、Point区間同期を移植
4. `index.html`の文言とボタン名を移植
5. Python 3.8を対象にする場合だけ`server.py`の型注釈互換化を移植
6. Windowsローカル測定とSSHリモート測定の既存処理を壊していないか確認

特に`service.py`と`app.js`は前バージョン側でも変更されている可能性があるため、ファイル丸ごとの置換ではなく関数単位でマージしてください。

## PC側Codexへの依頼文

以下をPC側Codexへそのまま渡せます。

```text
このフォルダの HANDOFF_20260728.md を読み、そこに記載された2026-07-28のJetson実機改修を、Git管理されている前バージョンへ移植してください。

前バージョン側の既存変更を保持し、ファイル丸ごとの上書きではなく関数単位でマージしてください。service.py、logger_core.py、static/app.js、static/index.htmlを重点的に比較し、Python 3.8、Windowsローカル、Jetsonローカル、SSHリモートの互換性を維持してください。

実行時生成物（.venv、logs、CSV、PNG、__pycache__）はコミットしないでください。移植後はHANDOFF_20260728.mdの推奨確認項目を可能な範囲でテストし、差分をレビューしてからコミット・pushしてください。
```

## 推奨確認項目

前バージョンへ反映後、最低限以下を確認してください。

1. Python 3.8でサーバーが起動する
2. Wi-Fi切断中にCSVへ`wifi_disconnected`が複数行記録される
3. Wi-Fi切断中も次のPointへ進める
4. 切断後に保存済みSSIDへ自動再接続する
5. BSSIDを測定開始前に切替・固定できる
6. BSSID固定後に同一SSID内で自動ローミングしない
7. BSSID固定解除後に`802-11-wireless.bssid`が空になる
8. Ping timeoutが赤橙、Wi-Fi切断が濃灰、欠測が明灰で描画される
9. Point欠番区間が消えず、No dataとして表示される
10. PNG出力にも状態凡例が含まれる
11. Windowsローカル測定とSSHリモート測定が従来どおり動作する
12. BSSID固定中に固定先が見えない場合、約5秒周期で可視性確認が行われる
13. BSSID固定中の再接続で別BSSIDへ接続しない
14. 測定を停止して再開始しても、前セッションのPoint座標が残らない
15. ログが増えた後半でも、Pointクリック直後にロガーのPoint番号が進む
16. 測定中は対象SSID以外の保存済みWi-Fiが`autoconnect=no`になる
17. 測定停止後に全Wi-Fiプロファイルの自動接続設定と優先度が元へ戻る
18. 手動SSID固定は測定停止後も維持され、解除ボタンで復元できる
19. サーバー異常終了後も残ったスナップショットから設定を復元できる

## 起動・停止に関する注意

作業時点では、サーバーをtmuxセッションで起動しています。

```bash
tmux list-sessions
tmux attach -t web-measurement-20260728
```

すでに9000番ポートで起動中に`./start_jetson.sh`を重ねて実行すると、以下のエラーになります。

```text
[Errno 98] address already in use
```

これは既存サーバーが動いているためで、アプリの異常ではありません。

## 2026-08-10 固定BSSID再接続の追加修正

`DB1-WRT-ATT4-1_CWFant__20260810_161014.csv`では16:13:47から切断し、
`events.csv`上は約15.25秒間隔で16回再接続を試行していました。しかし全試行が
`Fixed BSSID is not visible: 30:de:4b:4a:12:67`で終了し、電波状態が戻る復路でも
再接続しませんでした。測定終了後の手動スキャンでは同BSSIDをCH36・Signal 60で
検出し、TO-WAI2へ接続できることを確認しました。

原因候補は、`nmcli device wifi rescan`が新しいスキャン結果を公開する前に、
`--rescan no`のキャッシュを1回だけ読み、古い結果で「見えない」と判定していたことです。

追加修正:

1. 再スキャン要求後、固定BSSIDが見つかるまで最大3秒、0.5秒間隔でキャッシュを確認
2. 同一SSIDへ接続済みでも、保存済み固定BSSIDと一致するか
   `reconnect_local_wifi`で必ず検証
3. 別BSSIDへの接続を再接続成功として扱わない

変更対象:

- `web_measurement_app/logger_core.py`
- `web_measurement_app/service.py`
- `FEATURES.md`

この変更はWi-Fi切断中のバックグラウンド再接続だけに作用します。接続中のRSSI、
Ping、CSV数値、Point更新処理には変更を加えていません。

### ATT4-2での再確認

修正版で`DB1-WRT-ATT4-2_CWFant__20260810_173245.csv`を再測定しました。

- 17:37:20から再接続を開始
- 通常約5.2秒間隔で47回試行
- 通常の試行は約3.25秒で、追加した最大3秒のスキャン待機が動作
- P15は17:41:05～17:43:11の約126秒間待機したが再接続なし
- 全試行が固定BSSID `30:de:4b:4a:12:67`未検出
- NetworkManagerも17:37:20に`ssid-not-found`を記録
- 測定後の強制再スキャンでも対象BSSIDと`TO-WAI2` 5 GHzは不在
- 同時点で見えたのは`TO-WAI2-6GHz`と他SSID
- 保存済みTO-WAI2プロファイルのBSSID固定は正しく維持

この2回目は再接続処理が停止していたのではなく、固定対象APを無線スキャンで
検出できないため接続不能だったと判断します。対象APの電源・5 GHz無線状態、
またはATTを含むRF経路の受信感度を別途確認する必要があります。APが見えない状態で
固定BSSIDへ接続することはできません。

## PCへ持っていかないもの

以下は実行時生成物なので、前バージョンへのpush対象から除外してください。

```text
.venv/
logs/
*.csv
*.png
__pycache__/
```
